---
phase: 05-loading-states-ux
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/components/chem/AllFiltersView.tsx
  - frontend/src/components/chem/ClassyfireView.tsx
  - frontend/src/components/chem/CoconutPreProcessingView.tsx
  - frontend/src/components/chem/DescriptorsView.tsx
  - frontend/src/components/chem/ErtlFunctionalGroupView.tsx
  - frontend/src/components/chem/FixRadicalsView.tsx
  - frontend/src/components/chem/HOSECodeView.tsx
  - frontend/src/components/chem/NPlikenessView.tsx
  - frontend/src/components/chem/PubChemLookupView.tsx
  - frontend/src/components/chem/StandardizeView.tsx
  - frontend/src/components/chem/StandardizedTautomerView.tsx
  - frontend/src/components/chem/StereoisomersView.tsx
  - frontend/src/components/chem/StructureErrorView.tsx
  - frontend/src/components/chem/TanimotoView.tsx
  - frontend/src/components/convert/FormatConversionView.tsx
  - frontend/src/components/convert/Mol2DView.tsx
  - frontend/src/components/convert/Mol3DView.tsx
  - frontend/src/components/depict/Depict2DMultiView.tsx
  - frontend/src/components/depict/StructureVisualizerView.tsx
  - frontend/src/components/tools/StructureGenView.tsx
  - frontend/src/components/tools/SugarRemovalView.tsx
  - frontend/src/components/common/MoleculeCard.tsx
  - frontend/src/components/common/HighlightedMoleculeCard.tsx
  - frontend/src/components/common/LoadingScreen.tsx
autonomous: true
requirements:
  - LOAD-01
  - LOAD-02
  - LOAD-03
  - LOAD-04

must_haves:
  truths:
    - "No tool view uses LoadingScreen -- all show per-component glass skeletons in the output area only"
    - "Submit buttons show inline spinner icon + loading text when submitting, disabled during loading"
    - "Input area stays visible and usable during loading -- never covered by an overlay"
    - "Failed API calls show GlassErrorCard with contextual message and retry button in the output area"
    - "All 3 alert() calls replaced -- MoleculeCard and HighlightedMoleculeCard use toast.error, NPlikenessView uses popover or info expansion"
    - "Tools with no results show EmptyState component instead of blank space"
    - "LoadingScreen.tsx is deleted after all 21 usages removed"
  artifacts:
    - path: "frontend/src/components/common/LoadingScreen.tsx"
      provides: "DELETED -- no longer exists"
    - path: "frontend/src/components/chem/DescriptorsView.tsx"
      provides: "Example refactored tool view with GlassSkeleton + GlassErrorCard + inline spinner"
      contains: "GlassSkeleton|GlassErrorCard|ToolSkeleton"
  key_links:
    - from: "frontend/src/components/chem/DescriptorsView.tsx"
      to: "frontend/src/components/feedback/GlassSkeleton.tsx"
      via: "import { ToolSkeleton }"
      pattern: "import.*ToolSkeleton.*from.*feedback"
    - from: "frontend/src/components/chem/DescriptorsView.tsx"
      to: "frontend/src/components/feedback/GlassErrorCard.tsx"
      via: "import { GlassErrorCard }"
      pattern: "import.*GlassErrorCard.*from.*feedback"
    - from: "frontend/src/components/common/MoleculeCard.tsx"
      to: "sonner"
      via: "toast.error replacing alert()"
      pattern: "toast\\.error"
---

<objective>
Refactor all 21 tool view components from full-page LoadingScreen to per-component glass skeletons, inline submit button spinners, and GlassErrorCard for errors. Replace all 3 alert() calls with toast.error/popover. Delete LoadingScreen.tsx after migration.

Purpose: This is the core user-facing change -- transforms every API interaction from a modal overlay experience to a modern, non-blocking feedback pattern that keeps users oriented.
Output: 21 tool views refactored, 2 card components updated, LoadingScreen deleted.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-loading-states-ux/05-CONTEXT.md
@.planning/phases/05-loading-states-ux/05-RESEARCH.md
@.planning/phases/05-loading-states-ux/05-01-SUMMARY.md

<interfaces>
<!-- Components created in Plan 01 that this plan uses -->

From frontend/src/components/feedback/GlassSkeleton.tsx:
```typescript
interface GlassSkeletonProps {
  className?: string;
  variant?: "text" | "image" | "card" | "row" | "circle";
}
export const GlassSkeleton: React.FC<GlassSkeletonProps>;
```

From frontend/src/components/feedback/GlassErrorCard.tsx:
```typescript
interface GlassErrorCardProps {
  message: string;
  onRetry?: () => void;
  className?: string;
}
export const GlassErrorCard: React.FC<GlassErrorCardProps>;
```

From frontend/src/components/feedback/ToolSkeleton.tsx:
```typescript
interface ToolSkeletonProps {
  variant?: "descriptors" | "molecule" | "conversion" | "general";
  className?: string;
}
export const ToolSkeleton: React.FC<ToolSkeletonProps>;
```

From frontend/src/components/feedback/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  message?: string;
  icon?: React.ReactNode;
  className?: string;
}
export const EmptyState: React.FC<EmptyStateProps>;
```

From frontend/src/lib/error-messages.ts:
```typescript
export function getErrorMessage(domain: string, error: unknown): string;
```

From sonner:
```typescript
import { toast } from "sonner";
toast.error("message", { duration: 5000 });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor all 21 tool view components from LoadingScreen to glass skeletons + error cards</name>
  <files>
    frontend/src/components/chem/AllFiltersView.tsx
    frontend/src/components/chem/ClassyfireView.tsx
    frontend/src/components/chem/CoconutPreProcessingView.tsx
    frontend/src/components/chem/DescriptorsView.tsx
    frontend/src/components/chem/ErtlFunctionalGroupView.tsx
    frontend/src/components/chem/FixRadicalsView.tsx
    frontend/src/components/chem/HOSECodeView.tsx
    frontend/src/components/chem/NPlikenessView.tsx
    frontend/src/components/chem/PubChemLookupView.tsx
    frontend/src/components/chem/StandardizeView.tsx
    frontend/src/components/chem/StandardizedTautomerView.tsx
    frontend/src/components/chem/StereoisomersView.tsx
    frontend/src/components/chem/StructureErrorView.tsx
    frontend/src/components/chem/TanimotoView.tsx
    frontend/src/components/convert/FormatConversionView.tsx
    frontend/src/components/convert/Mol2DView.tsx
    frontend/src/components/convert/Mol3DView.tsx
    frontend/src/components/depict/Depict2DMultiView.tsx
    frontend/src/components/depict/StructureVisualizerView.tsx
    frontend/src/components/tools/StructureGenView.tsx
    frontend/src/components/tools/SugarRemovalView.tsx
  </files>
  <action>
Apply the following mechanical refactoring pattern to ALL 21 tool view files. Each follows the same before/after pattern:

**For each file:**

A. **Remove LoadingScreen import**, replace with:
   ```typescript
   import { ToolSkeleton } from "@/components/feedback/ToolSkeleton";
   import { GlassErrorCard } from "@/components/feedback/GlassErrorCard";
   import { EmptyState } from "@/components/feedback/EmptyState";
   import { getErrorMessage } from "@/lib/error-messages";
   import { Loader2 } from "lucide-react";
   ```
   (Loader2 may already be imported -- check first. Only add imports that are not already present.)

B. **Update the error state setter** in the catch block:
   - BEFORE: `setError(\`Error ...: ${err.message || "..."}\`)`
   - AFTER: `setError(getErrorMessage("DOMAIN", err))`
   - Domain mapping:
     - chem/*.tsx -> "chem"
     - convert/*.tsx -> "convert"
     - depict/*.tsx -> "depict"
     - tools/*.tsx -> "tools"

C. **Update submit button** to show inline spinner when loading:
   - BEFORE: `{loading ? "Loading..." : "Submit"}`
   - AFTER: `{loading ? (<><Loader2 className="mr-2 h-4 w-4 animate-spin" />Processing...</>) : (<><OriginalIcon className="mr-2 h-4 w-4" />Original Text</>)}`
   - Keep the existing button icon when not loading. The loading text should be contextual:
     - Descriptor tools: "Calculating..."
     - Structure tools: "Processing..."
     - Conversion tools: "Converting..."
     - Depiction tools: "Generating..."
     - General: "Analyzing..."

D. **Replace LoadingScreen usage** with ToolSkeleton in the output area:
   - BEFORE: `{loading && <LoadingScreen text="..." />}`
   - AFTER: `{loading && !result && <ToolSkeleton variant="VARIANT" />}`
   - Variant mapping:
     - DescriptorsView, AllFiltersView, CoconutPreProcessingView, HOSECodeView, NPlikenessView, PubChemLookupView: "descriptors"
     - StandardizeView, FixRadicalsView, StandardizedTautomerView, StructureErrorView, StereoisomersView, ClassyfireView: "molecule"
     - FormatConversionView, Mol2DView, Mol3DView: "conversion"
     - TanimotoView, ErtlFunctionalGroupView, StructureGenView, SugarRemovalView, Depict2DMultiView, StructureVisualizerView: "general"
   - The skeleton should appear in the output area only, NOT covering the input form

E. **Replace error display** with GlassErrorCard:
   - BEFORE: `{error && (<div className="p-4 rounded-md bg-red-50..."><AlertCircle .../><span>{error}</span></div>)}`
   - AFTER: `{error && !loading && <GlassErrorCard message={error} onRetry={() => { setError(null); }} />}`
   - The onRetry clears the error state. If the component has a ref to the input (inputRef, smilesRef, etc.), also focus it.

F. **Add EmptyState** for initial empty state where components show a static message:
   - BEFORE: `{!result && !loading && !error && (<div className="bg-blue-50...">Enter input...</div>)}`
   - AFTER: `{!result && !loading && !error && <EmptyState message="Enter a SMILES string to calculate descriptors" />}`
   - Customize the message per tool. If no initial state exists, add one.

**NPlikenessView special case:**
   - The `alert()` on line 156 shows NP-likeness score range info. This is NOT an error -- it's informational.
   - Replace with an expandable info section: add a `showInfo` boolean state, toggle it on button click, render a glass-bold info box below the button with the score range info when showInfo is true.
   - Do NOT use toast for this -- it's not a side-effect.

**Important implementation notes:**
- Keep ALL existing functionality intact (form submission, result display, AddToCompareButton, etc.)
- Do NOT change the loading/error state management pattern -- each component already correctly manages its own loading/error state
- Do NOT use AppContext.isLoading -- each tool manages local state
- If a component already has a contextual loading text on its button, adapt it (don't replace "Calculating..." with generic "Processing...")
  </action>
  <verify>
    <automated>cd frontend && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All 21 tool views import ToolSkeleton/GlassErrorCard instead of LoadingScreen
    - All 21 tool views show inline spinner on submit button during loading
    - All 21 tool views show ToolSkeleton in output area during loading (not full-page overlay)
    - All 21 tool views show GlassErrorCard with contextual error message on failure
    - NPlikenessView info alert replaced with expandable info section
    - No tool view imports LoadingScreen
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace alert() in MoleculeCard and HighlightedMoleculeCard + delete LoadingScreen</name>
  <files>
    frontend/src/components/common/MoleculeCard.tsx
    frontend/src/components/common/HighlightedMoleculeCard.tsx
    frontend/src/components/common/LoadingScreen.tsx
  </files>
  <action>
1. **MoleculeCard.tsx** (line ~184):
   - Add `import { toast } from "sonner";` at top
   - Replace `alert("Failed to download structure. Please try again later.");`
   - With: `toast.error("Failed to download structure", { description: "Please try again later.", duration: 5000 });`

2. **HighlightedMoleculeCard.tsx** (line ~315):
   - Add `import { toast } from "sonner";` at top
   - Replace `alert("Failed to download structure. Please try again later.");`
   - With: `toast.error("Failed to download structure", { description: "Please try again later.", duration: 5000 });`

3. **Delete LoadingScreen.tsx:**
   - Verify no remaining imports: `grep -r "LoadingScreen" frontend/src/ --include="*.tsx" --include="*.ts"`
   - If zero results, delete `frontend/src/components/common/LoadingScreen.tsx`
   - If any remain, fix them first

4. Verify no remaining alert() calls in the codebase:
   - `grep -r "alert(" frontend/src/ --include="*.tsx" --include="*.ts"` should return zero results
  </action>
  <verify>
    <automated>cd frontend && npx vitest run && grep -r "LoadingScreen" src/ --include="*.tsx" --include="*.ts" | wc -l && grep -r "alert(" src/ --include="*.tsx" --include="*.ts" | wc -l</automated>
  </verify>
  <done>
    - MoleculeCard uses toast.error instead of alert() for download failure
    - HighlightedMoleculeCard uses toast.error instead of alert() for download failure
    - LoadingScreen.tsx is deleted from the codebase
    - Zero remaining alert() calls in frontend source
    - Zero remaining LoadingScreen imports in frontend source
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run` -- all tests pass
- `cd frontend && npm run build` -- production build succeeds
- `grep -r "LoadingScreen" frontend/src/ --include="*.tsx" --include="*.ts"` returns 0 matches
- `grep -r "alert(" frontend/src/ --include="*.tsx" --include="*.ts"` returns 0 matches
- Every tool view imports from `@/components/feedback/` not from `../common/LoadingScreen`
</verification>

<success_criteria>
- All 21 tool views refactored from LoadingScreen to per-component glass skeletons
- Submit buttons show inline Loader2 spinner during loading
- Error states use GlassErrorCard with contextual messages (not raw error text)
- All 3 alert() calls replaced (2 with toast.error, 1 with info expansion)
- LoadingScreen.tsx deleted
- Production build and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-loading-states-ux/05-02-SUMMARY.md`
</output>
