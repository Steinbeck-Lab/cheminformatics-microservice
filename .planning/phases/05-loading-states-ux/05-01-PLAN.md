---
phase: 05-loading-states-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/tailwind.css
  - frontend/src/components/feedback/GlassSkeleton.tsx
  - frontend/src/components/feedback/GlassErrorCard.tsx
  - frontend/src/components/feedback/ToolSkeleton.tsx
  - frontend/src/components/feedback/EmptyState.tsx
  - frontend/src/components/feedback/RouteLoadingFallback.tsx
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/lib/error-messages.ts
  - frontend/src/services/api.ts
  - frontend/package.json
  - frontend/src/__tests__/components/glass-skeleton.test.tsx
  - frontend/src/__tests__/components/glass-error-card.test.tsx
  - frontend/src/__tests__/components/sonner.test.tsx
  - frontend/src/__tests__/components/tool-loading.test.tsx
autonomous: true
requirements:
  - LOAD-01
  - LOAD-02
  - LOAD-03
  - LOAD-04

must_haves:
  truths:
    - "GlassSkeleton renders with frosted glass appearance and left-to-right shimmer animation"
    - "GlassErrorCard displays user-friendly message with red/amber accent border and retry button"
    - "ToolSkeleton renders content-shaped placeholders matching tool output layouts (image, text rows, property table)"
    - "Sonner Toaster renders glass-styled toasts in bottom-right corner"
    - "Axios interceptor catches network/5xx errors and shows deduplicated toast notifications"
    - "Error messages are contextual per domain (chem, convert, depict, tools, ocsr) -- no raw error text shown"
  artifacts:
    - path: "frontend/src/components/feedback/GlassSkeleton.tsx"
      provides: "Base glass shimmer skeleton component"
      exports: ["GlassSkeleton"]
    - path: "frontend/src/components/feedback/GlassErrorCard.tsx"
      provides: "Error card with retry button"
      exports: ["GlassErrorCard"]
    - path: "frontend/src/components/feedback/ToolSkeleton.tsx"
      provides: "Content-shaped skeleton variants for tool output areas"
      exports: ["ToolSkeleton"]
    - path: "frontend/src/components/feedback/EmptyState.tsx"
      provides: "Empty state for tools with no results"
      exports: ["EmptyState"]
    - path: "frontend/src/components/feedback/RouteLoadingFallback.tsx"
      provides: "Suspense fallback for route-level code splitting"
      exports: ["RouteLoadingFallback"]
    - path: "frontend/src/components/ui/sonner.tsx"
      provides: "Glass-styled Toaster wrapper"
      exports: ["Toaster"]
    - path: "frontend/src/lib/error-messages.ts"
      provides: "Contextual error message mapping"
      exports: ["getErrorMessage"]
    - path: "frontend/src/services/api.ts"
      provides: "Axios interceptor with toast notifications for network/5xx errors"
  key_links:
    - from: "frontend/src/components/feedback/GlassSkeleton.tsx"
      to: "frontend/src/styles/tailwind.css"
      via: "animate-shimmer CSS class"
      pattern: "animate-shimmer"
    - from: "frontend/src/services/api.ts"
      to: "sonner"
      via: "toast.error() in response interceptor"
      pattern: "toast\\.error"
    - from: "frontend/src/components/feedback/GlassErrorCard.tsx"
      to: "frontend/src/lib/error-messages.ts"
      via: "contextual message prop"
      pattern: "getErrorMessage"
---

<objective>
Create the foundation feedback components (GlassSkeleton, GlassErrorCard, ToolSkeleton, EmptyState, RouteLoadingFallback), install Sonner with glass-styled Toaster, create contextual error message mapping, and wire the Axios response interceptor for global network/5xx error toasts. Add shimmer animation CSS to tailwind.css.

Purpose: All downstream tool view refactoring depends on these components existing. This plan creates the complete feedback component library and error handling infrastructure.
Output: 7 new component/utility files, updated tailwind.css with shimmer keyframes, updated api.ts with toast interceptor, 4 test files.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-loading-states-ux/05-CONTEXT.md
@.planning/phases/05-loading-states-ux/05-RESEARCH.md

@frontend/src/styles/tailwind.css
@frontend/src/services/api.ts
@frontend/src/lib/utils.ts
@frontend/src/context/AppContext.tsx
@frontend/src/components/ui/button.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From frontend/src/lib/utils.ts:
```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
```

From frontend/src/context/AppContext.tsx (relevant excerpt):
```typescript
export function useAppContext(): { isDarkMode: boolean; toggleDarkMode: () => void; ... }
```

From frontend/src/services/api.ts:
```typescript
// Existing Axios instance with request/response interceptors (stubs ready)
// Response interceptor currently logs errors in DEV mode only
import axios, { AxiosInstance, InternalAxiosRequestConfig, AxiosResponse, AxiosError } from "axios";
const api: AxiosInstance = axios.create({ baseURL: API_URL, timeout: 30000 });
export default api;
```

Existing glass utilities in tailwind.css:
- `glass` - standard glass (blur 16px light, 24px dark)
- `glass-bold` - primary glass surface (blur 24px, translucent bg)
- `glass-mobile` - reduced blur (12px) for mobile GPU performance
- `clay-interactive` - claymorphism for interactive elements
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Sonner + shimmer CSS + feedback components + error messages</name>
  <files>
    frontend/package.json
    frontend/src/styles/tailwind.css
    frontend/src/components/feedback/GlassSkeleton.tsx
    frontend/src/components/feedback/GlassErrorCard.tsx
    frontend/src/components/feedback/ToolSkeleton.tsx
    frontend/src/components/feedback/EmptyState.tsx
    frontend/src/components/feedback/RouteLoadingFallback.tsx
    frontend/src/components/ui/sonner.tsx
    frontend/src/lib/error-messages.ts
  </files>
  <action>
1. Install dependencies:
   ```bash
   cd frontend && npm install sonner react-error-boundary
   ```

2. Add shimmer keyframes to `tailwind.css` in the `@theme` block (after existing `--animate-bounce-slow`):
   ```
   --animate-shimmer: shimmer 2s ease-in-out infinite;
   ```
   Then add a new `@keyframes shimmer` block after the existing keyframes:
   ```css
   @keyframes shimmer {
     0% { transform: translateX(-100%); }
     100% { transform: translateX(100%); }
   }
   ```

3. Create `src/components/feedback/GlassSkeleton.tsx`:
   - Props: `className?: string`, `variant?: "text" | "image" | "card" | "row" | "circle"`
   - Base: `relative overflow-hidden rounded-lg bg-white/10 dark:bg-slate-700/20 backdrop-blur-sm`
   - Variant sizing: text=h-4 w-full, image=h-48 w-full, card=h-32 w-full, row=h-6 w-3/4, circle=h-12 w-12 rounded-full
   - Shimmer overlay: absolute inset-0 div with `animate-shimmer bg-gradient-to-r from-transparent via-white/20 dark:via-white/5 to-transparent`
   - Use cn() for class merging

4. Create `src/components/feedback/GlassErrorCard.tsx`:
   - Props: `message: string`, `onRetry?: () => void`, `className?: string`
   - Container: `glass-bold rounded-xl p-6 border-l-4 border-l-red-500 dark:border-l-red-400` with `role="alert"`
   - Layout: flex items-start gap-3 with AlertTriangle icon (lucide-react) in red, message text, and optional Retry button
   - Retry button: shadcn Button variant="ghost" size="sm" with RotateCcw icon and "Try again" text
   - Use motion.div for entrance animation (fade + slide up)

5. Create `src/components/feedback/ToolSkeleton.tsx`:
   - Props: `variant?: "descriptors" | "molecule" | "conversion" | "general"`, `className?: string`
   - Each variant is a composition of GlassSkeleton components matching the tool output layout:
     - `descriptors`: image placeholder (h-48) + 6 property rows (alternating w-full and w-3/4)
     - `molecule`: large image placeholder (h-64) + 2 text lines + 4 property rows
     - `conversion`: 3 text lines (code-like, w-full) + 1 shorter line (w-1/2)
     - `general`: 2 text lines + image placeholder + 3 rows
   - Wrap in glass-bold container with p-6

6. Create `src/components/feedback/EmptyState.tsx`:
   - Props: `message?: string`, `icon?: React.ReactNode`, `className?: string`
   - Container: glass-bold rounded-xl p-8 text-center
   - Default icon: Beaker from lucide-react (h-12 w-12 text-muted-foreground)
   - Default message: "Enter input above and submit to see results"
   - Muted text styling

7. Create `src/components/feedback/RouteLoadingFallback.tsx`:
   - Full-width centered container with padding
   - Shows 3 GlassSkeleton blocks: a "card" variant, then 2 "text" variants
   - Wrapped in motion.div with fade-in animation
   - NOT a fixed overlay -- sits inline in the main content area

8. Create `src/components/ui/sonner.tsx` (glass-styled Toaster wrapper):
   - Import Toaster as SonnerToaster from "sonner"
   - Import useAppContext for isDarkMode
   - Position: "bottom-right"
   - Theme: isDarkMode ? "dark" : "light"
   - toastOptions.duration: 3000 (default, success)
   - toastOptions.classNames with glass styling:
     - toast: `!backdrop-blur-xl !bg-white/70 dark:!bg-slate-800/70 !border !border-white/20 dark:!border-slate-600/30 !shadow-lg`
     - error: `!border-l-4 !border-l-red-500` (duration override to 5000 via individual toast calls)
     - success: `!border-l-4 !border-l-green-500`
     - description: `!text-muted-foreground`

9. Create `src/lib/error-messages.ts`:
   - ERROR_MESSAGES record organized by domain: chem, convert, depict, tools, ocsr, network
   - Each domain has: default, parse (optional), timeout (optional) messages
   - Messages are user-friendly: "Could not analyze this molecule", "Could not convert this structure", etc.
   - Export `getErrorMessage(domain: string, error: unknown): string` function that inspects error for timeout/parse hints and returns appropriate message
   - Console.error the technical details inside getErrorMessage (only in dev mode)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>
    - GlassSkeleton renders with shimmer animation class and 5 size variants
    - GlassErrorCard renders with red accent border, AlertTriangle icon, message, and optional retry button
    - ToolSkeleton renders content-shaped skeletons for 4 tool output types
    - EmptyState renders with icon and message for tools with no results
    - RouteLoadingFallback renders inline skeleton blocks for route loading
    - Sonner Toaster renders glass-styled toasts positioned bottom-right
    - error-messages.ts exports getErrorMessage with per-domain contextual messages
    - shimmer keyframes defined in tailwind.css
    - sonner and react-error-boundary installed in package.json
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Axios interceptor + test stubs for all feedback components</name>
  <files>
    frontend/src/services/api.ts
    frontend/src/__tests__/components/glass-skeleton.test.tsx
    frontend/src/__tests__/components/glass-error-card.test.tsx
    frontend/src/__tests__/components/sonner.test.tsx
    frontend/src/__tests__/components/tool-loading.test.tsx
  </files>
  <action>
1. Update `src/services/api.ts` response interceptor:
   - Add `import { toast } from "sonner";` at top
   - In the error handler:
     - Network errors (no response) or 5xx (status >= 500): `toast.error("Service temporarily unavailable", { id: "network-error", description: "Please try again in a moment.", duration: 5000 })`
     - Use `id: "network-error"` to deduplicate concurrent failures (Sonner replaces toast with same ID)
     - Rate limiting (429): `toast.error("Too many requests", { id: "rate-limit", description: "Please wait before trying again.", duration: 5000 })`
     - Keep existing DEV console logging
     - 4xx errors are NOT toasted -- they propagate to per-component error handling
   - Return `Promise.reject(error)` for all errors (preserves downstream catch)

2. Create test stubs (Nyquist Wave 0):

   `src/__tests__/components/glass-skeleton.test.tsx`:
   - Test: "renders with shimmer animation class" -- render GlassSkeleton, assert animate-shimmer class present
   - Test: "renders text variant by default" -- render GlassSkeleton, assert h-4 height class
   - Test: "renders image variant" -- render with variant="image", assert h-48 class
   - Test: "accepts custom className" -- render with className="custom", assert class merged

   `src/__tests__/components/glass-error-card.test.tsx`:
   - Test: "renders error message" -- render GlassErrorCard with message, assert text visible
   - Test: "renders retry button when onRetry provided" -- render with onRetry callback, assert "Try again" button visible
   - Test: "does not render retry button when onRetry omitted" -- render without onRetry, assert no retry button
   - Test: "calls onRetry when retry button clicked" -- render with vi.fn(), click retry, assert called

   `src/__tests__/components/sonner.test.tsx`:
   - Test: "Toaster component renders without crashing" -- render Toaster within AppProvider
   - Test: "toast.error displays error message" -- call toast.error, assert toast appears (may need act())

   `src/__tests__/components/tool-loading.test.tsx`:
   - Test: "ToolSkeleton renders descriptors variant" -- render ToolSkeleton variant="descriptors", assert multiple skeleton blocks
   - Test: "ToolSkeleton renders molecule variant" -- render variant="molecule", assert image placeholder present
   - Test: "EmptyState renders default message" -- render EmptyState, assert default text
   - Test: "RouteLoadingFallback renders skeleton blocks" -- render component, assert skeleton elements present

   All tests should be real assertions (not it.todo) -- the components exist from Task 1.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/__tests__/components/glass-skeleton.test.tsx src/__tests__/components/glass-error-card.test.tsx src/__tests__/components/sonner.test.tsx src/__tests__/components/tool-loading.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Axios response interceptor shows toast.error for network/5xx errors with dedup ID
    - Axios response interceptor shows toast.error for 429 rate limiting
    - 4xx errors propagate to per-component handling without toast
    - All 4 test files pass with real assertions (not stubs)
    - glass-skeleton tests verify shimmer class and variant sizing
    - glass-error-card tests verify message, retry button, and click handler
    - sonner test verifies Toaster renders
    - tool-loading tests verify ToolSkeleton variants, EmptyState, and RouteLoadingFallback
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run` -- all tests pass
- `cd frontend && npm run build` -- production build succeeds with no errors
- GlassSkeleton, GlassErrorCard, ToolSkeleton, EmptyState, RouteLoadingFallback all importable from feedback/
- Sonner Toaster importable from ui/sonner
- getErrorMessage importable from lib/error-messages
- api.ts interceptor imports toast from sonner
</verification>

<success_criteria>
- 7 new feedback/UI component files created and functional
- Shimmer animation defined in tailwind.css and renders visually
- Axios interceptor catches 5xx/network errors with toast, deduplicates concurrent failures
- 4 test files pass with real assertions covering LOAD-01 through LOAD-04
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-loading-states-ux/05-01-SUMMARY.md`
</output>
