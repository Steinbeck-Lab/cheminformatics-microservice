---
phase: 01-vite-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.js
  - frontend/index.html
  - frontend/public/index.html
  - frontend/src/services/api.js
  - frontend/src/context/AppContext.js
  - frontend/src/components/chem/MoleculeCard.jsx
  - frontend/src/components/chem/HighlightedMoleculeCard.jsx
  - frontend/src/pages/ConvertPage.jsx
  - frontend/src/components/tools/StructureDrawView.jsx
  - frontend/src/components/convert/RInChIView.jsx
  - frontend/src/components/convert/InChIView.jsx
  - frontend/postcss.config.js
  - frontend/tailwind.config.js
  - frontend/.env
  - frontend/.env.production
  - frontend/.env.local.example
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
  - BUILD-03
  - BUILD-04
  - BUILD-06

must_haves:
  truths:
    - "Running `npm run dev` in frontend/ starts the Vite dev server on port 3000 and the app loads in browser"
    - "Running `npm run build` in frontend/ produces a dist/ directory with no errors"
    - "`npm audit` reports zero vulnerabilities"
    - "package.json contains no `overrides` section"
    - "No file in src/ contains the string `process.env`"
    - "All 5 files that referenced REACT_APP_API_URL now use import.meta.env.VITE_API_URL"
    - "All 3 files that referenced PUBLIC_URL now use direct paths or import.meta.env.BASE_URL"
  artifacts:
    - path: "frontend/vite.config.js"
      provides: "Vite build configuration with React SWC plugin and dev server settings"
      contains: "defineConfig"
    - path: "frontend/index.html"
      provides: "Vite entry HTML file at project root"
      contains: "script type=\"module\" src=\"/src/index.js\""
    - path: "frontend/.env"
      provides: "Default environment variables for local development"
      contains: "VITE_API_URL"
    - path: "frontend/package.json"
      provides: "Updated dependencies and scripts without react-scripts or overrides"
      min_lines: 30
  key_links:
    - from: "frontend/index.html"
      to: "frontend/src/index.js"
      via: "script type=module src"
      pattern: "src=\"/src/index.js\""
    - from: "frontend/src/services/api.js"
      to: "import.meta.env.VITE_API_URL"
      via: "env variable reference"
      pattern: "import\\.meta\\.env\\.VITE_API_URL"
    - from: "frontend/vite.config.js"
      to: "@vitejs/plugin-react-swc"
      via: "plugin import"
      pattern: "import react from.*plugin-react-swc"
---

<objective>
Replace Create React App with Vite, migrate all environment variables, update all dependencies to latest stable, and eliminate all security vulnerabilities.

Purpose: This is the foundation of the entire frontend overhaul. CRA is deprecated and the source of all 16 npm audit vulnerabilities. Removing it and switching to Vite unblocks every subsequent phase.
Output: A working Vite-based frontend that builds, serves, and has zero audit vulnerabilities.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vite-migration/01-CONTEXT.md
@.planning/phases/01-vite-migration/01-RESEARCH.md

@frontend/package.json
@frontend/public/index.html
@frontend/Dockerfile
@frontend/postcss.config.js
@frontend/tailwind.config.js
@docker-compose.yml

<interfaces>
<!-- Key files that need env var migration. Executor needs to know exact patterns to find and replace. -->

Files with REACT_APP_API_URL (5 files):
- frontend/src/services/api.js
- frontend/src/context/AppContext.js
- frontend/src/components/chem/MoleculeCard.jsx
- frontend/src/components/chem/HighlightedMoleculeCard.jsx
- (1 more file to confirm via grep -- research says StandardizeView.jsx but pages listing shows ConvertPage.jsx; grep at execution time)

Files with process.env.PUBLIC_URL (3 files):
- frontend/src/components/tools/StructureDrawView.jsx
- frontend/src/components/convert/RInChIView.jsx
- frontend/src/components/convert/InChIView.jsx

Files with process.env.NODE_ENV (1 file):
- frontend/src/services/api.js
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uninstall CRA, install Vite stack, restructure package.json</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <action>
  Execute the following steps in the frontend/ directory:

  1. **Uninstall CRA packages:**
     ```
     npm uninstall react-scripts web-vitals
     ```

  2. **Install Vite core (devDependencies):**
     ```
     npm install -D vite@^6.4.1 @vitejs/plugin-react-swc@^4.2.3 vite-plugin-checker@^0.11.0
     ```

  3. **Upgrade TypeScript (devDependency):**
     ```
     npm install -D typescript@^5.9.3
     ```

  4. **Install build utilities:**
     ```
     npm install -D rollup-plugin-visualizer@^7.0.1
     ```

  5. **Move testing-library packages from dependencies to devDependencies:**
     ```
     npm uninstall @testing-library/jest-dom @testing-library/react @testing-library/user-event
     ```
     (These will be re-installed as devDeps in Plan 02 along with Vitest.)

  6. **Update package.json manually:**
     - Remove the entire `"overrides"` section (BUILD-03)
     - Remove the entire `"eslintConfig"` section (replaced by eslint.config.mjs in Plan 02)
     - Remove the entire `"browserslist"` section (Vite uses build.target instead)
     - Replace `"scripts"` with:
       ```json
       {
         "dev": "vite",
         "build": "vite build",
         "preview": "vite preview",
         "test": "vitest run",
         "test:watch": "vitest",
         "test:coverage": "vitest run --coverage",
         "lint": "eslint \"src/**/*.{js,jsx}\"",
         "lint:fix": "eslint \"src/**/*.{js,jsx}\" --fix",
         "format": "prettier --write \"src/**/*.{js,jsx,json,css,md}\"",
         "format:check": "prettier --check \"src/**/*.{js,jsx,json,css,md}\"",
         "build:analyze": "ANALYZE=true vite build",
         "prepare": "husky"
       }
       ```
     - Remove `"homepage": ""` (CRA-specific, not needed by Vite)

  7. **Run npm install** to regenerate lockfile cleanly after all changes.

  8. **Run `npm audit`** to verify zero vulnerabilities. If any remain, investigate and fix by upgrading the parent dependency (no overrides per user decision).

  Note: The `"test"`, `"lint"`, and `"prepare"` scripts will reference tools installed in Plan 02. That is fine -- they do not need to work yet, just be defined.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && cat package.json | grep -c '"overrides"' | grep -q '^0$' && echo "No overrides: PASS" && npm audit --audit-level=low 2>&1 | tail -5</automated>
  </verify>
  <done>react-scripts and web-vitals removed. Vite 6.x, plugin-react-swc, vite-plugin-checker, TypeScript 5.x, and rollup-plugin-visualizer installed as devDeps. No overrides, no eslintConfig, no browserslist in package.json. npm audit reports zero vulnerabilities.</done>
</task>

<task type="auto">
  <name>Task 2: Create Vite config, migrate index.html, migrate all env vars</name>
  <files>frontend/vite.config.js, frontend/index.html, frontend/src/services/api.js, frontend/src/context/AppContext.js, frontend/src/components/chem/MoleculeCard.jsx, frontend/src/components/chem/HighlightedMoleculeCard.jsx, frontend/src/components/tools/StructureDrawView.jsx, frontend/src/components/convert/RInChIView.jsx, frontend/src/components/convert/InChIView.jsx, frontend/.env, frontend/.env.production, frontend/.env.local.example, frontend/postcss.config.js, frontend/tailwind.config.js</files>
  <action>
  **Step 1: Create frontend/vite.config.js**

  Create vite.config.js at the frontend root with:
  - `@vitejs/plugin-react-swc` plugin
  - `vite-plugin-checker` with typescript: true and eslint config (eslint lintCommand: `eslint "./src/**/*.{js,jsx}"`, useFlatConfig: true)
  - `server.port: 3000`
  - `server.proxy` for `/v1` and `/latest` to `http://localhost:8000` (improves dev experience by avoiding CSP issues -- Claude's discretion decision: YES, add proxy since the CSP meta tag hardcodes production URLs and doesn't include localhost)
  - `build.sourcemap: 'hidden'`
  - `build.outDir: 'dist'`
  - Conditionally include `rollup-plugin-visualizer` when `process.env.ANALYZE` is set (this is Node.js server-side, process.env is valid here)

  Use the exact pattern from 01-RESEARCH.md Pattern 1, with the proxy addition.

  **Step 2: Move and transform index.html**

  Copy frontend/public/index.html to frontend/index.html (Vite requires it at project root). Then transform:
  - Replace all `%PUBLIC_URL%/` with `/` (3 occurrences: favicon, apple-touch-icon, manifest)
  - Add `<script type="module" src="/src/index.js"></script>` before the closing `</body>` tag
  - Keep ALL existing content intact: meta tags, Matomo analytics script, 3Dmol.js CDN script, CSP meta tag, Open Graph tags, Twitter tags, Google Fonts link
  - Do NOT delete the original public/index.html yet (keep it until confirmed working, then delete)

  **Step 3: Migrate environment variables in source files**

  First, run `grep -rn "process\.env" frontend/src/` to find ALL occurrences. Then migrate each:

  For `process.env.REACT_APP_API_URL` (5 files):
  - Replace with `import.meta.env.VITE_API_URL`
  - Keep the same fallback values

  For `process.env.PUBLIC_URL` (3 files):
  - Replace with direct absolute path `/` or use `import.meta.env.BASE_URL` (which defaults to '/')
  - For iframe src patterns like `${process.env.PUBLIC_URL}/standalone/index.html`, simplify to `/standalone/index.html`

  For `process.env.NODE_ENV` (1 file: api.js):
  - Replace `process.env.NODE_ENV !== "production"` with `import.meta.env.DEV` (Vite boolean helper)

  **Step 4: Create environment files**

  Create `frontend/.env`:
  ```
  VITE_API_URL=http://localhost:8000/latest
  ```

  Update `frontend/.env.production` (should already exist):
  - Change `REACT_APP_API_URL` to `VITE_API_URL`
  - Keep the production URL value

  Create/update `frontend/.env.local.example`:
  ```
  # Copy this file to .env.local and customize for your local setup
  VITE_API_URL=http://localhost:8000/latest
  ```

  **Step 5: Remove GENERATE_SOURCEMAP references**

  If `.env` or `.env.production` contains `GENERATE_SOURCEMAP=false`, remove it. This is now handled by `build.sourcemap: 'hidden'` in vite.config.js.

  **Step 6: Update Tailwind/PostCSS config (if needed)**

  The existing `postcss.config.js` and `tailwind.config.js` use `module.exports` (CommonJS). This is fine -- Vite processes PostCSS config files and handles CJS config. Do NOT convert these to ESM yet (that happens in Phase 3 Tailwind v4 migration). Verify they work with `npm run build`.

  **Step 7: Delete the original public/index.html**

  After all migrations are complete and verified, delete `frontend/public/index.html` to avoid confusion. The canonical index.html is now at `frontend/index.html`.

  **Step 8: Final grep verification**

  Run `grep -rn "process\.env" frontend/src/` -- must return zero results. Any remaining `process.env` reference will cause a runtime "process is not defined" error.

  Run `grep -rn "REACT_APP_" frontend/` (excluding node_modules) -- must return zero results.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && grep -rn "process\.env" src/ && echo "FAIL: process.env found" && exit 1 || echo "PASS: no process.env" && grep -rn "REACT_APP_" src/ .env* 2>/dev/null && echo "FAIL: REACT_APP_ found" && exit 1 || echo "PASS: no REACT_APP_" && test -f index.html && echo "PASS: index.html at root" && test -f vite.config.js && echo "PASS: vite.config.js exists" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>vite.config.js created with SWC plugin, checker, proxy, and hidden sourcemaps. index.html moved to root with Vite entry script. All 9 process.env references migrated across 8 files. Environment files created with VITE_* prefix. `npm run build` succeeds and produces dist/ directory. Zero grep hits for process.env or REACT_APP_ in source.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run build` -- succeeds with zero errors, produces dist/ directory
2. `cd frontend && npm audit --audit-level=low` -- reports zero vulnerabilities
3. `grep -rn "process\.env" frontend/src/` -- returns nothing
4. `grep -rn "REACT_APP_" frontend/src/ frontend/.env*` -- returns nothing
5. `grep -c "overrides" frontend/package.json` -- returns 0
6. `test -f frontend/index.html && test -f frontend/vite.config.js` -- both exist
7. `cd frontend && npm run dev` -- starts dev server on port 3000 (manual spot check: pages load)
</verification>

<success_criteria>
- Vite dev server starts on port 3000 and serves the app
- Production build completes with zero errors into dist/
- npm audit reports zero vulnerabilities
- package.json has no overrides, no eslintConfig, no browserslist
- Zero process.env references remain in frontend source code
- All REACT_APP_* replaced with VITE_* equivalents
</success_criteria>

<output>
After completion, create `.planning/phases/01-vite-migration/01-01-SUMMARY.md`
</output>
