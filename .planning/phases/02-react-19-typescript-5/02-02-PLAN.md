---
phase: 02-react-19-typescript-5
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/pages/AboutPage.tsx
  - frontend/src/pages/HomePage.tsx
  - frontend/src/pages/ChemPage.tsx
  - frontend/src/pages/ConvertPage.tsx
  - frontend/src/pages/DepictPage.tsx
  - frontend/src/pages/ToolsPage.tsx
  - frontend/src/pages/OCSRPage.tsx
  - frontend/src/pages/PrivacyPolicy.tsx
  - frontend/src/pages/TermsOfService.tsx
  - frontend/src/components/common/Header.tsx
  - frontend/src/components/common/Footer.tsx
  - frontend/src/components/common/LoadingScreen.tsx
  - frontend/src/components/depict/Depict2DMultiView.tsx
  - frontend/src/components/ocsr/OCRView.tsx
  - frontend/src/components/tools/StructureGenView.tsx
autonomous: false
requirements: [FRAME-05]

must_haves:
  truths:
    - "The motion package is installed and framer-motion is removed from package.json"
    - "All 15 files using framer-motion now import from motion/react"
    - "Page transitions animate smoothly when navigating between routes"
    - "Tab-based pages (ChemPage, DepictPage, ConvertPage, ToolsPage) show LayoutGroup animations"
    - "Scroll animations on AboutPage work correctly"
    - "The production build succeeds with the motion package"
    - "All 9 pages render correctly with no console errors"
  artifacts:
    - path: "frontend/package.json"
      provides: "motion package in dependencies, framer-motion removed"
      contains: "motion"
    - path: "frontend/src/pages/ChemPage.tsx"
      provides: "Motion imports from motion/react"
      contains: "motion/react"
    - path: "frontend/src/components/common/Header.tsx"
      provides: "Motion imports from motion/react"
      contains: "motion/react"
    - path: "frontend/src/components/common/Footer.tsx"
      provides: "Motion imports from motion/react with enhanced scroll animations"
      contains: "motion/react"
  key_links:
    - from: "frontend/src/pages/*.tsx"
      to: "motion/react"
      via: "import statement"
      pattern: 'from "motion/react"'
    - from: "frontend/src/components/common/Header.tsx"
      to: "motion/react"
      via: "import statement"
      pattern: 'from "motion/react"'
    - from: "frontend/package.json"
      to: "motion@^12.35.2"
      via: "dependencies"
      pattern: '"motion"'
---

<objective>
Migrate from the framer-motion package to the motion package across all 15 files, enhance existing animations with Motion 12 features (improved spring physics, smoother transitions), and visually verify all 9 pages work correctly.

Purpose: The motion package is the official successor to framer-motion (30.7k GitHub stars, 3.6M weekly npm downloads). The API is identical -- this is an import path change with targeted animation quality improvements. The user explicitly wants "smooth cool animation" that works in all browsers.

Output: All animation imports use motion/react, framer-motion removed, animations enhanced where beneficial, all 9 pages visually verified.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-react-19-typescript-5/02-CONTEXT.md
@.planning/phases/02-react-19-typescript-5/02-RESEARCH.md
@.planning/phases/02-react-19-typescript-5/02-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, all files are .tsx/.ts. These are the 15 files with framer-motion imports that need migration. -->

Files with framer-motion imports (all now .tsx after Plan 01):
1. src/pages/AboutPage.tsx - motion, useScroll, useTransform
2. src/pages/ChemPage.tsx - motion, AnimatePresence, LayoutGroup
3. src/pages/ConvertPage.tsx - motion, AnimatePresence, LayoutGroup
4. src/pages/DepictPage.tsx - motion, AnimatePresence, LayoutGroup
5. src/pages/HomePage.tsx - motion
6. src/pages/OCSRPage.tsx - motion
7. src/pages/PrivacyPolicy.tsx - motion
8. src/pages/TermsOfService.tsx - motion
9. src/pages/ToolsPage.tsx - motion, AnimatePresence, LayoutGroup
10. src/components/common/Header.tsx - motion, AnimatePresence, LayoutGroup
11. src/components/common/Footer.tsx - motion, useAnimation, useInView
12. src/components/common/LoadingScreen.tsx - motion
13. src/components/depict/Depict2DMultiView.tsx - motion, AnimatePresence, LayoutGroup
14. src/components/ocsr/OCRView.tsx - motion, AnimatePresence
15. src/components/tools/StructureGenView.tsx - motion, AnimatePresence

Import migration is purely a path change:
  BEFORE: import { motion, AnimatePresence } from "framer-motion";
  AFTER:  import { motion, AnimatePresence } from "motion/react";

The API (components, hooks, props) is identical between framer-motion and motion packages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate framer-motion to motion package with animation enhancements</name>
  <files>
    frontend/package.json
    frontend/src/pages/AboutPage.tsx
    frontend/src/pages/HomePage.tsx
    frontend/src/pages/ChemPage.tsx
    frontend/src/pages/ConvertPage.tsx
    frontend/src/pages/DepictPage.tsx
    frontend/src/pages/ToolsPage.tsx
    frontend/src/pages/OCSRPage.tsx
    frontend/src/pages/PrivacyPolicy.tsx
    frontend/src/pages/TermsOfService.tsx
    frontend/src/components/common/Header.tsx
    frontend/src/components/common/Footer.tsx
    frontend/src/components/common/LoadingScreen.tsx
    frontend/src/components/depict/Depict2DMultiView.tsx
    frontend/src/components/ocsr/OCRView.tsx
    frontend/src/components/tools/StructureGenView.tsx
  </files>
  <action>
    **Step 1: Swap packages**

    Run from frontend/:
    ```
    npm install motion@^12.35.2
    npm uninstall framer-motion
    ```

    **Step 2: Update all 15 import paths**

    In every file listed above, change:
    ```
    from "framer-motion"  ->  from "motion/react"
    ```
    The API is identical -- no prop or component changes needed (Pattern 5 from research).

    **Step 3: Enhance animations (targeted improvements per research recommendations)**

    Apply the following TARGETED enhancements. DO NOT rewrite working animations -- only upgrade where quality genuinely improves:

    a) **Spring physics upgrade** (pages with tab indicators: ChemPage, DepictPage, ConvertPage, ToolsPage, Depict2DMultiView):
       - Where hardcoded `duration` transitions exist on layout animations (tab pill indicators, content transitions), replace with spring-based transitions: `transition={{ type: "spring", stiffness: 500, damping: 30 }}` for snappy tab switches.
       - Where layout animations already use springs, keep as-is.

    b) **Page transition smoothing** (HomePage, AboutPage, PrivacyPolicy, TermsOfService, OCSRPage):
       - Improve initial page mount animations: use `transition={{ type: "spring", stiffness: 100, damping: 20 }}` instead of hardcoded duration for hero/content entrance, giving a more natural deceleration feel.
       - Keep exit animations as-is (they work well).

    c) **DO NOT CHANGE** (per research recommendations):
       - Footer canvas particles -- fragile, works well
       - Header mobile menu animations -- fragile, works well
       - LoadingScreen animations -- works well
       - useScroll/useTransform on AboutPage -- already sophisticated, keep as-is
       - LayoutGroup structure on any page -- keep existing group logic

    **Step 4: Verify compilation**

    Run `cd frontend && npx tsc --noEmit && npm test && npm run build` to confirm everything compiles and builds correctly with the motion package.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test && npm run build && grep -r "framer-motion" src/ | wc -l | grep -q "^0$" && echo "PASS: motion migrated, no framer-motion refs" || echo "FAIL"</automated>
  </verify>
  <done>Motion package installed, framer-motion removed, all 15 files import from "motion/react", spring physics enhanced on tab transitions and page entries, build and tests pass, zero references to framer-motion remain in source</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 9 pages and animations</name>
  <files>None -- verification only, no files modified</files>
  <action>
    Human verifies the complete Phase 2 migration: React 19.2.4 + TypeScript 5.x + motion package with enhanced animations. All 61 source files converted to TypeScript, all animation imports migrated to motion/react, spring physics upgraded on tab transitions and page entries.

    Start the dev server: `cd frontend && npm run dev`
    Then open http://localhost:3000 in your browser and verify:

    1. **HomePage** -- Page loads, hero section animates in smoothly, navigation links work
    2. **ChemPage** -- All tabs switch with smooth spring animation, tab indicator pill slides correctly, content transitions work
    3. **ConvertPage** -- Tab switching with spring animations, format conversion views load
    4. **DepictPage** -- Tab switching with spring animations, 2D/3D depiction views load
    5. **ToolsPage** -- Tab switching with spring animations, tool views load
    6. **OCSRPage** -- Page loads with smooth entrance animation
    7. **AboutPage** -- Scroll animations work (useScroll/useTransform), content reveals on scroll
    8. **PrivacyPolicy** -- Page loads with smooth entrance
    9. **TermsOfService** -- Page loads with smooth entrance

    Also verify:
    - Open browser DevTools Console -- check for zero React errors, zero TypeScript-related runtime errors
    - Header mobile menu (resize to mobile) -- hamburger menu opens/closes with animation
    - Footer -- particles and scroll animations work
    - Loading screen -- appears briefly on initial load with animation
    - Run `cd frontend && npm run build` -- confirm production build succeeds
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <done>User has visually confirmed all 9 pages render correctly with smooth animations, no console errors, and production build succeeds</done>
</task>

</tasks>

<verification>
1. `cd frontend && npm ls motion` -- motion@12.x is installed
2. `cd frontend && npm ls framer-motion 2>&1` -- framer-motion is NOT installed (should error)
3. `cd frontend && grep -r "framer-motion" src/ | wc -l` -- Returns 0
4. `cd frontend && grep -r "motion/react" src/ | wc -l` -- Returns 15 (all files migrated)
5. `cd frontend && npx tsc --noEmit && npm test && npm run build` -- All pass
6. Visual: All 9 pages render with animations, no console errors
</verification>

<success_criteria>
- motion@^12.35.2 is installed and framer-motion is completely removed
- All 15 files import from "motion/react" instead of "framer-motion"
- Tab-based pages show spring-physics tab transitions (snappy, natural)
- Page entrance animations use spring physics (natural deceleration)
- All 9 pages render correctly with no console errors
- Production build succeeds
- User has visually approved the animations
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-19-typescript-5/02-02-SUMMARY.md`
</output>
