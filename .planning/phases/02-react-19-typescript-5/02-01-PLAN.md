---
phase: 02-react-19-typescript-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/eslint.config.mjs
  - frontend/index.html
  - frontend/src/types/api.ts
  - frontend/src/types/molecule.ts
  - frontend/src/types/global.d.ts
  - frontend/src/index.tsx
  - frontend/src/App.tsx
  - frontend/src/context/AppContext.tsx
  - frontend/src/hooks/useApiCall.ts
  - frontend/src/hooks/useMolecule.ts
  - frontend/src/services/api.ts
  - frontend/src/services/chemService.ts
  - frontend/src/services/convertService.ts
  - frontend/src/services/depictService.ts
  - frontend/src/services/ocsrService.ts
  - frontend/src/services/toolsService.ts
  - frontend/src/utils/inchiUtils.ts
  - frontend/src/utils/rinchiUtils.ts
  - frontend/src/__tests__/setup.ts
  - frontend/src/__tests__/App.test.tsx
  - frontend/src/pages/*.tsx
  - frontend/src/components/**/*.tsx
autonomous: true
requirements: [FRAME-01, FRAME-03]

must_haves:
  truths:
    - "React 19.2.4 is installed and the app renders without crashing"
    - "TypeScript 5.x compiles the entire project with no type errors (npx tsc --noEmit passes)"
    - "All 61 source files are renamed from .js/.jsx to .ts/.tsx"
    - "The production build succeeds (npm run build)"
    - "The existing smoke test passes (npm test)"
    - "ESLint passes on .ts/.tsx files with TypeScript parser"
  artifacts:
    - path: "frontend/tsconfig.json"
      provides: "Root TypeScript config with project references"
      contains: "references"
    - path: "frontend/tsconfig.app.json"
      provides: "App TypeScript config with strict mode and React JSX"
      contains: "react-jsx"
    - path: "frontend/tsconfig.node.json"
      provides: "Node TypeScript config for vite.config.ts"
      contains: "vite.config.ts"
    - path: "frontend/vite.config.ts"
      provides: "Vite config converted from .mjs, without treat-js-as-jsx plugin"
      exports: ["default"]
    - path: "frontend/src/types/global.d.ts"
      provides: "Global type declarations for 3Dmol, Vite env vars"
      contains: "$3Dmol"
    - path: "frontend/src/types/api.ts"
      provides: "Shared API response type definitions"
    - path: "frontend/src/types/molecule.ts"
      provides: "Molecule data type definitions"
    - path: "frontend/src/index.tsx"
      provides: "Entry point renamed from .jsx"
    - path: "frontend/package.json"
      provides: "Updated dependencies (React 19, @types/react@19) and script globs (.ts/.tsx)"
  key_links:
    - from: "frontend/index.html"
      to: "frontend/src/index.tsx"
      via: "script src attribute"
      pattern: 'src="/src/index.tsx"'
    - from: "frontend/vite.config.ts"
      to: "frontend/src/__tests__/setup.ts"
      via: "setupFiles config"
      pattern: "setup.ts"
    - from: "frontend/eslint.config.mjs"
      to: "**/*.{ts,tsx}"
      via: "files glob pattern"
      pattern: "ts,tsx"
    - from: "frontend/package.json"
      to: "react@^19.2.4"
      via: "dependencies"
      pattern: "19.2.4"
---

<objective>
Upgrade React 18 to 19.2.4, convert the entire JavaScript codebase to TypeScript (.ts/.tsx), set up TypeScript infrastructure (tsconfig, type definitions, ESLint TypeScript support), and update all dev tooling to target .ts/.tsx files.

Purpose: This is the foundation for the entire phase -- React 19 and TypeScript must be working together before motion migration can proceed. The user's locked decision requires atomic conversion of all 61 files in a single operation.

Output: All 61 source files converted to TypeScript, React 19 installed, tsconfig strict mode enabled, ESLint with TypeScript parser, all quality gates passing.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-react-19-typescript-5/02-CONTEXT.md
@.planning/phases/02-react-19-typescript-5/02-RESEARCH.md
@.planning/phases/01-vite-migration/01-01-SUMMARY.md
@.planning/phases/01-vite-migration/01-02-SUMMARY.md

<interfaces>
<!-- Key existing files that will be converted. Executor needs to understand current structure. -->

From frontend/vite.config.mjs (will become vite.config.ts):
- Contains treat-js-as-jsx custom plugin (REMOVE in conversion)
- Contains optimizeDeps.esbuildOptions.loader for .js (REMOVE in conversion)
- Contains setupFiles: "./src/__tests__/setup.js" (UPDATE to .ts)
- Contains SWC React plugin with jsxRuntime: "automatic"

From frontend/eslint.config.mjs (stays .mjs but content updates):
- Currently targets files: ["**/*.{js,jsx}"] (UPDATE to ts,tsx)
- Uses eslint-plugin-react, react-hooks, jsx-a11y, prettier
- Has relaxed rules (8 rules set to warn) -- KEEP as-is
- Needs: typescript-eslint import and tseslint.configs.recommended spread
- Needs: no-unused-vars replaced by @typescript-eslint/no-unused-vars

From frontend/package.json:
- lint-staged globs: "src/**/*.{js,jsx}" (UPDATE to ts,tsx)
- scripts lint/format globs reference .js/.jsx (UPDATE to ts,tsx)
- React 18.2.0 in dependencies (UPGRADE to 19.2.4)
- @headlessui/react in dependencies (REMOVE -- dead dependency per research)
- typescript 5.9.3 already in devDependencies

From frontend/index.html:
- script src="/src/index.jsx" (UPDATE to /src/index.tsx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create TypeScript infrastructure, rename all files</name>
  <files>
    frontend/package.json
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/tsconfig.node.json
    frontend/src/types/global.d.ts
    frontend/src/types/api.ts
    frontend/src/types/molecule.ts
    frontend/index.html
    All 61 .js/.jsx files renamed to .ts/.tsx
  </files>
  <action>
    **Step 1: Install/uninstall dependencies**
    Run from frontend/:
    ```
    npm install react@^19.2.4 react-dom@^19.2.4
    npm install -D @types/react@^19 @types/react-dom@^19 @types/dompurify typescript-eslint@^8
    npm uninstall @headlessui/react
    ```
    @headlessui/react is removed because research confirmed it is NOT imported in any source file -- dead dependency.

    **Step 2: Create TypeScript config files**

    Create `tsconfig.json` (root references file):
    ```json
    {
      "references": [
        { "path": "./tsconfig.app.json" },
        { "path": "./tsconfig.node.json" }
      ],
      "files": []
    }
    ```

    Create `tsconfig.app.json` with `strict: true` per research recommendation (shadcn/ui in Phase 4 expects strict). Use exact config from 02-RESEARCH.md Pattern 1 -- target ES2020, module ESNext, moduleResolution bundler, jsx react-jsx, noUnusedLocals true, noUnusedParameters true, noUncheckedIndexedAccess true, resolveJsonModule true, esModuleInterop true.

    Create `tsconfig.node.json` targeting vite.config.ts -- use exact config from 02-RESEARCH.md Pattern 1.

    **Step 3: Create type definition files**

    Create `src/types/global.d.ts` with:
    - `/// <reference types="vite/client" />` for CSS/SVG/asset imports
    - `declare const $3Dmol` with createViewer signature (loaded via CDN in index.html)
    - `interface ImportMetaEnv` with `readonly VITE_API_URL: string`
    - `interface ImportMeta` with `readonly env: ImportMetaEnv`

    Create `src/types/molecule.ts` with molecule data types derived from existing useMolecule hook and AppContext patterns (RecentMolecule interface, MoleculeData interface).

    Create `src/types/api.ts` with shared API response types derived from existing service JSDoc annotations (StructureErrorResult, DescriptorResult, ConversionResult, DepictionResult, etc.).

    **Step 4: Rename all 61 files atomically**

    Use git mv for all renames to preserve history:
    - 9 pages: src/pages/*.jsx -> *.tsx
    - 35 components: src/components/**/*.jsx -> *.tsx
    - 6 services: src/services/*.js -> *.ts
    - 2 hooks: src/hooks/*.js -> *.ts
    - 1 context: src/context/AppContext.js -> .tsx
    - 2 utils: src/utils/*.js -> *.ts
    - 1 app: src/App.js -> App.tsx
    - 1 entry: src/index.jsx -> src/index.tsx
    - 2 tests: src/__tests__/setup.js -> .ts, src/__tests__/App.test.jsx -> .tsx

    This is the "atomic conversion" per user's locked decision.

    **Step 5: Update index.html**

    Change `<script type="module" src="/src/index.jsx">` to `src="/src/index.tsx"`. This MUST happen immediately after the rename or the app will show a blank page (Pitfall 1 from research).

    **Step 6: Update package.json scripts and lint-staged**

    Update all script globs from .js/.jsx to .ts/.tsx:
    - lint-staged: `"src/**/*.{ts,tsx}"` (was .js,.jsx)
    - scripts.lint: `eslint "src/**/*.{ts,tsx}"` (was .js,.jsx)
    - scripts.lint:fix: `eslint "src/**/*.{ts,tsx}" --fix`
    - scripts.format: `prettier --write "src/**/*.{ts,tsx,json,css,md}"`
    - scripts.format:check: `prettier --check "src/**/*.{ts,tsx,json,css,md}"`
    - Add scripts.typecheck: `tsc --noEmit`
  </action>
  <verify>
    <automated>cd frontend && ls tsconfig.json tsconfig.app.json tsconfig.node.json src/types/global.d.ts src/types/api.ts src/types/molecule.ts && find src -name "*.js" -o -name "*.jsx" | wc -l | grep -q "^0$" && echo "PASS: All files renamed, tsconfig exists" || echo "FAIL"</automated>
  </verify>
  <done>All 61 files renamed to .ts/.tsx, tsconfig created with strict mode, type definition files created, React 19.2.4 installed, dead dependencies removed, index.html script src updated, package.json globs updated</done>
</task>

<task type="auto">
  <name>Task 2: Add TypeScript types to all source files, update configs, pass compilation</name>
  <files>
    frontend/vite.config.ts
    frontend/eslint.config.mjs
    frontend/src/context/AppContext.tsx
    frontend/src/hooks/useApiCall.ts
    frontend/src/hooks/useMolecule.ts
    frontend/src/services/api.ts
    frontend/src/services/chemService.ts
    frontend/src/services/convertService.ts
    frontend/src/services/depictService.ts
    frontend/src/services/ocsrService.ts
    frontend/src/services/toolsService.ts
    frontend/src/utils/inchiUtils.ts
    frontend/src/utils/rinchiUtils.ts
    frontend/src/App.tsx
    frontend/src/index.tsx
    frontend/src/__tests__/setup.ts
    frontend/src/__tests__/App.test.tsx
    frontend/src/pages/*.tsx
    frontend/src/components/**/*.tsx
  </files>
  <action>
    **Step 1: Convert vite.config.mjs to vite.config.ts**

    Create `vite.config.ts` with the exact content from 02-RESEARCH.md Code Examples section:
    - Remove the `treat-js-as-jsx` custom plugin entirely (no longer needed with .tsx files)
    - Remove the `optimizeDeps.esbuildOptions.loader` section (no longer needed)
    - Update `setupFiles` to `"./src/__tests__/setup.ts"` (Pitfall 5 from research)
    - Keep: SWC React plugin, server proxy, build config, test config
    Then delete the old vite.config.mjs.

    **Step 2: Update ESLint config for TypeScript**

    Update `eslint.config.mjs` to match the config from 02-RESEARCH.md Code Examples:
    - Add `import tseslint from "typescript-eslint"` (unified package)
    - Spread `...tseslint.configs.recommended` after `js.configs.recommended`
    - Update files glob from `**/*.{js,jsx}` to `**/*.{ts,tsx}`
    - Replace `no-unused-vars` rule with `@typescript-eslint/no-unused-vars` (same config: argsIgnorePattern ^_, varsIgnorePattern ^_)
    - Add `"no-unused-vars": "off"` to prevent conflict with TS version
    - Keep ALL existing relaxed rules (they apply to existing code)

    **Step 3: Type the service layer**

    Convert each service file using Pattern 3 from research:
    - `api.ts`: Add AxiosInstance, InternalAxiosRequestConfig, AxiosResponse types per research Code Examples
    - `chemService.ts`: Add interfaces for each API function's return type (StructureErrorResult, DescriptorResult, etc.). Derive types from existing JSDoc annotations and parameter defaults.
    - `convertService.ts`: Add ConversionResult interface
    - `depictService.ts`: Add DepictionResult interface
    - `ocsrService.ts`: Add OCRResult interface
    - `toolsService.ts`: Add ToolResult interfaces (SugarRemoval, InChI, etc.)

    For each service function: type the parameters explicitly, type the return as Promise<T>, use the generic Axios get/post with the response type.

    **Step 4: Type the hooks**

    Convert using Pattern 4 from research:
    - `useApiCall.ts`: Add UseApiCallOptions<T> and UseApiCallReturn<T> generics per research example
    - `useMolecule.ts`: Add typed return interface based on existing hook's return values

    **Step 5: Type the context**

    Convert `AppContext.tsx` using Pattern 2 from research:
    - Add AppContextValue interface with all context fields
    - Add RecentMolecule, ApiConfig interfaces
    - Type createContext<AppContextValue | undefined>(undefined)
    - Add typed useAppContext hook with runtime check

    **Step 6: Type all component and page files**

    For each of the ~45 component and page .tsx files:
    - Add props interfaces where components receive props (Pattern 6 from research)
    - Use explicit interfaces for complex props (components with 3+ props)
    - Use inline typing for simple components (0-2 props)
    - Type event handlers: React.ChangeEvent<HTMLInputElement>, React.FormEvent<HTMLFormElement>, etc.
    - Type state: useState<string>, useState<boolean>, useState<SomeType | null>, etc.
    - Type refs: useRef<HTMLDivElement>(null), etc.
    - DO NOT over-type: let TypeScript infer return types, map/filter results, simple assignments
    - Use `unknown` instead of `any` where types are unclear. Use `// @ts-expect-error` with explanation only as last resort.

    **Step 7: Type utility files**

    - `inchiUtils.ts`: Type function parameters and return values
    - `rinchiUtils.ts`: Type function parameters and return values

    **Step 8: Update test files**

    - `setup.ts`: Minimal changes (just the rename should work)
    - `App.test.tsx`: Update import path if needed, ensure test compiles

    **Step 9: Compile and fix**

    Run `npx tsc --noEmit` and fix any remaining type errors. If error count exceeds 100, temporarily set `noUnusedLocals: false` and `noUnusedParameters: false` in tsconfig.app.json (enforce via ESLint warnings instead per research Pitfall 2).

    CRITICAL: The framer-motion imports remain as `from "framer-motion"` in this task. Do NOT change them -- the motion package migration happens in Plan 02.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm test && npm run build && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>TypeScript compiles the entire project with zero type errors (npx tsc --noEmit passes), the smoke test passes (npm test), production build succeeds (npm run build), and ESLint with TypeScript parser passes (npm run lint). All service, hook, context, component, and page files have proper TypeScript types.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- TypeScript compilation with zero errors
2. `cd frontend && npm test` -- Vitest smoke test passes
3. `cd frontend && npm run build` -- Production build succeeds
4. `cd frontend && npm run lint` -- ESLint with TypeScript parser passes
5. `cd frontend && find src -name "*.js" -o -name "*.jsx" | wc -l` -- Returns 0 (no JS/JSX files remain)
6. `cd frontend && node -e "const p=require('./package.json'); console.log(p.dependencies.react)"` -- Shows ^19.2.4
7. `cd frontend && ls tsconfig.json tsconfig.app.json tsconfig.node.json` -- All three tsconfig files exist
</verification>

<success_criteria>
- React 19.2.4 and react-dom 19.2.4 are installed (npm ls react shows 19.x)
- TypeScript strict mode compiles the entire project with zero errors
- All 61 source files are .ts/.tsx (zero .js/.jsx remain in src/)
- vite.config.ts exists and treat-js-as-jsx plugin is removed
- ESLint runs with typescript-eslint parser on .ts/.tsx files
- Production build produces dist/ without errors
- Smoke test passes in Vitest
- @headlessui/react is removed from package.json
</success_criteria>

<output>
After completion, create `.planning/phases/02-react-19-typescript-5/02-01-SUMMARY.md`
</output>
