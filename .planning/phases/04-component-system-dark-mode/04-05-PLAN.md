---
phase: 04-component-system-dark-mode
plan: 05
type: execute
wave: 1
depends_on: ["04-04"]
files_modified:
  - frontend/src/pages/OCSRPage.tsx
  - frontend/src/pages/HomePage.tsx
  - frontend/src/pages/AboutPage.tsx
  - frontend/src/components/common/Footer.tsx
autonomous: true
gap_closure: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
  - COMP-07
  - THEME-01
  - THEME-02
  - THEME-03
  - THEME-04
  - THEME-05
must_haves:
  truths:
    - "All 9 pages use shadcn/ui Button for button-styled elements -- no hand-rolled gradient anchor or Link elements acting as buttons"
    - "OCSRPage info cards and sidebar info box use shadcn Card/CardContent instead of raw divs"
    - "HomePage CTA, Guides, and API Docs buttons use shadcn Button with asChild"
    - "HomePage feature cards wrap content in shadcn Card"
    - "AboutPage button-styled motion.a links use shadcn Button with asChild"
    - "Footer resource cards use shadcn Card instead of raw motion.a containers"
  artifacts:
    - path: "frontend/src/pages/OCSRPage.tsx"
      provides: "shadcn Button + Card migration for OCSR page"
      contains: "import { Button } from"
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "shadcn Button + Card migration for home page"
      contains: "import { Button } from"
    - path: "frontend/src/pages/AboutPage.tsx"
      provides: "shadcn Button migration for remaining motion.a buttons"
      contains: "Button asChild"
    - path: "frontend/src/components/common/Footer.tsx"
      provides: "shadcn Card migration for resource links"
      contains: "import { Card"
  key_links:
    - from: "frontend/src/pages/OCSRPage.tsx"
      to: "@/components/ui/button"
      via: "import { Button }"
      pattern: "import.*Button.*from.*@/components/ui/button"
    - from: "frontend/src/pages/OCSRPage.tsx"
      to: "@/components/ui/card"
      via: "import { Card, CardContent }"
      pattern: "import.*Card.*from.*@/components/ui/card"
    - from: "frontend/src/pages/HomePage.tsx"
      to: "@/components/ui/button"
      via: "import { Button }"
      pattern: "import.*Button.*from.*@/components/ui/button"
    - from: "frontend/src/pages/HomePage.tsx"
      to: "@/components/ui/card"
      via: "import { Card }"
      pattern: "import.*Card.*from.*@/components/ui/card"
---

<objective>
Close the two remaining VERIFICATION.md gaps: OCSRPage and HomePage still use hand-rolled button-like anchors and card-like divs instead of shadcn/ui Button and Card. Additionally, fix the same pattern in AboutPage (3 instances) and Footer (4 resource cards) for true COMP-07 consistency across all 9 pages.

Purpose: Fully satisfy COMP-02 (all buttons use shadcn Button), COMP-03 (all cards use shadcn Card), and COMP-07 (consistent usage across all 9 pages) which are currently marked PARTIAL in verification.
Output: All 4 files updated with shadcn/ui components, zero hand-rolled button-styled or card-styled elements remaining anywhere in the codebase.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-component-system-dark-mode/04-RESEARCH.md
@.planning/phases/04-component-system-dark-mode/04-VERIFICATION.md

<interfaces>
<!-- shadcn/ui Button component (from src/components/ui/button.tsx) -->
```typescript
function Button({
  className,
  variant = "default",   // "default" | "destructive" | "outline" | "secondary" | "ghost" | "link"
  size = "default",      // "default" | "xs" | "sm" | "lg" | "icon" | "icon-xs" | "icon-sm" | "icon-lg"
  asChild = false,       // when true, renders child as root via Radix Slot
  ...props
}): JSX.Element
export { Button, buttonVariants };
```

<!-- shadcn/ui Card component (from src/components/ui/card.tsx) -->
```typescript
function Card({ className, ...props }): JSX.Element
// Default classes: "flex flex-col gap-6 rounded-xl border bg-card py-6 text-card-foreground shadow-sm"
function CardContent({ className, ...props }): JSX.Element
// Default classes: "px-6"
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent };
```

<!-- Established pattern: motion.div wrapper + Button asChild (from Header.tsx lines 203-214) -->
```typescript
// motion.div carries animation, Button asChild delegates rendering to child anchor/Link
<motion.div variants={variant} whileHover="hover">
  <Button asChild size="lg" className="h-auto ...overrides">
    <a href="..." target="_blank" rel="noopener noreferrer">
      Link text <Icon />
    </a>
  </Button>
</motion.div>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate OCSRPage and HomePage to shadcn Button and Card</name>
  <files>frontend/src/pages/OCSRPage.tsx, frontend/src/pages/HomePage.tsx</files>
  <action>
**OCSRPage.tsx** -- Add imports and migrate 5 elements:

1. Add imports at top: `import { Button } from "@/components/ui/button";` and `import { Card, CardContent } from "@/components/ui/card";`

2. DECIMER button (lines 103-113): Replace `<motion.a>` with `<motion.div variants={buttonVariant} whileHover="hover">` wrapper around `<Button asChild size="lg" className="h-auto flex items-center justify-center px-6 py-4 bg-linear-to-r from-blue-600 to-blue-700 hover:from-blue-500 hover:to-blue-600 text-white font-semibold rounded-lg shadow-lg w-full md:w-auto text-lg">` containing `<a href="https://decimer.ai" target="_blank" rel="noopener noreferrer">`. Move the span text and ExternalLink icon inside the `<a>`.

3. MARCUS button (lines 114-124): Same pattern as DECIMER but with purple gradient: `from-purple-600 to-purple-700 hover:from-purple-500 hover:to-purple-600`, href="https://marcus.decimer.ai".

4. DECIMER info card (lines 133-148): Keep existing `<motion.div variants={headerItemVariants}>` as wrapper. Replace the inner styling `className="bg-white dark:bg-slate-800/80 rounded-lg p-5 border border-slate-200 dark:border-slate-700/50 shadow-md"` with `<Card className="bg-white dark:bg-slate-800/80 rounded-lg border-slate-200 dark:border-slate-700/50 shadow-md py-0 gap-0">` and wrap content in `<CardContent className="p-5">`. The motion.div keeps its variants; the Card only provides semantic structure and theming.

5. MARCUS info card (lines 149-167): Same Card + CardContent pattern as DECIMER card.

6. Sidebar info box (line 210-274): Replace `<div className="bg-slate-100 dark:bg-slate-800/60 rounded-lg p-5 border border-slate-200 dark:border-slate-700">` with `<Card className="bg-slate-100 dark:bg-slate-800/60 rounded-lg border-slate-200 dark:border-slate-700 py-0 gap-0">` and wrap all inner content in `<CardContent className="p-5">`.

**HomePage.tsx** -- Add imports and migrate buttons + cards:

1. Add imports: `import { Button } from "@/components/ui/button";` and `import { Card } from "@/components/ui/card";`

2. Primary CTA (lines 228-237): Replace `<Link to="/depict" className="group primary-button w-full sm:w-auto relative inline-flex ...">` with `<Button asChild size="lg" className="group primary-button w-full sm:w-auto relative h-auto inline-flex items-center justify-center px-7 py-3 sm:px-9 sm:py-4 text-base sm:text-lg font-semibold text-white bg-linear-to-r from-sky-600 to-cyan-500 dark:from-blue-600 dark:to-cyan-500 rounded-xl shadow-lg hover:shadow-xl hover:brightness-110 dark:hover:shadow-cyan-500/30 transition-all duration-300 ease-out transform hover:scale-[1.06] focus:outline-hidden focus:ring-4 ring-offset-2 ring-offset-slate-100 dark:ring-offset-gray-950 focus:ring-cyan-500/50 dark:focus:ring-cyan-400/50">` wrapping `<Link to="/depict">`. The inner spans stay inside the Link. Already inside a motion.div so no extra wrapper needed.

3. Guides button (lines 239-249): Replace `<a href="..." className="group w-full sm:w-auto ...">` with `<Button asChild variant="outline" size="lg" className="group w-full sm:w-auto relative h-auto inline-flex items-center justify-center px-7 py-3 sm:px-9 sm:py-4 text-base sm:text-lg font-semibold text-slate-700 dark:text-slate-200 bg-white/90 dark:bg-slate-800/80 backdrop-blur-md border-slate-300 dark:border-slate-700 hover:bg-white dark:hover:bg-slate-700/90 hover:border-slate-400 dark:hover:border-slate-500 rounded-xl shadow-md hover:shadow-lg dark:hover:shadow-slate-700/40 transition-all duration-300 ease-out transform hover:scale-[1.06] focus:outline-hidden focus:ring-4 focus:ring-slate-400/50 dark:focus:ring-slate-600/50 ring-offset-2 ring-offset-slate-100 dark:ring-offset-gray-950">` wrapping `<a href="https://docs.api.naturalproducts.net/introduction.html" target="_blank" rel="noopener noreferrer">`. Inner spans stay inside the `<a>`.

4. API Docs button (lines 250-260): Same pattern as Guides button but with href="https://api.naturalproducts.net/latest/docs" and BookOpen icon.

5. Feature cards (lines 284-317, in features.map): Wrap the `<Link>` in a `<Card className="feature-card-enhanced group relative flex flex-col h-full min-h-[270px] sm:min-h-[290px] bg-white/80 dark:bg-slate-800/75 backdrop-blur-xl border border-transparent rounded-3xl shadow-lg dark:shadow-xl hover:shadow-xl dark:hover:shadow-blue-900/30 overflow-hidden transform-style-3d p-0 gap-0 transition-all duration-400 ease-out">`. Move the `feature-card-enhanced` class and all structural/visual classes from Link to Card. The Link gets reduced to just `className="flex flex-col h-full p-4 sm:p-6"` with its existing style prop. CRITICAL: Keep `feature-card-enhanced` on Card so the `::before` pseudo-element border reveal effect continues to work.

6. Recent molecule cards (lines 354-366): Replace the `<div className="relative bg-white/80 dark:bg-slate-800/70 backdrop-blur-lg sm:backdrop-blur-xl rounded-2xl shadow-lg ...">` with `<Card className="relative bg-white/80 dark:bg-slate-800/70 backdrop-blur-lg sm:backdrop-blur-xl rounded-2xl shadow-lg dark:shadow-xl border-slate-200 dark:border-slate-700/80 overflow-hidden transition-all duration-300 hover:border-slate-300 dark:hover:border-slate-600 hover:bg-slate-50 dark:hover:bg-slate-700/80 p-1 py-0 gap-0">`. Keep the radial gradient overlay div and MoleculeCard inside.

IMPORTANT: Put ALL styling overrides on Button's className, not on the child element. The child (a or Link) should only have structural props (href, to, target, rel).

WARNING: Do NOT use motion.create(Button) -- use motion.div wrapper pattern per research.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>OCSRPage.tsx imports Button and Card from @/components/ui/. All 2 motion.a buttons converted to motion.div + Button asChild + a. All 3 card containers converted to Card + CardContent. HomePage.tsx imports Button and Card. Primary CTA uses Button asChild + Link. Both secondary buttons use Button asChild variant="outline" + a. All 5 feature cards wrapped in Card. All 3 recent molecule card containers use Card. All existing motion animations preserved. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate AboutPage button-styled links and Footer resource cards</name>
  <files>frontend/src/pages/AboutPage.tsx, frontend/src/components/common/Footer.tsx</files>
  <action>
**AboutPage.tsx** -- Already imports Button. Migrate 2 button-styled motion.a elements (the 3rd is a funder logo grid, NOT a button):

1. "Natural Products Online" button (lines 613-624): Replace `<motion.a href="https://naturalproducts.net" ... variants={buttonVariant} whileHover="hover">` with `<motion.div variants={buttonVariant} whileHover="hover">` wrapper around `<Button asChild size="lg" className="h-auto inline-flex items-center justify-center px-8 py-4 bg-linear-to-r from-blue-600 to-indigo-700 hover:from-blue-500 hover:to-indigo-600 text-white font-semibold rounded-lg shadow-lg text-lg">` containing `<a href="https://naturalproducts.net" target="_blank" rel="noopener noreferrer">`. Move the Atom icon, text span, and ExternalLink icon inside the `<a>`.

2. "api.naturalproducts.net" button (lines 658-668): Replace `<motion.a ... whileHover={{ y: -5, scale: 1.03 }} transition={{ duration: 0.2 }}>` with `<motion.div whileHover={{ y: -5, scale: 1.03 }} transition={{ duration: 0.2 }}>` wrapper around `<Button asChild size="lg" className="h-auto inline-flex items-center px-8 py-4 rounded-lg bg-linear-to-r from-blue-600 to-indigo-600 dark:from-blue-500 dark:to-indigo-500 text-white font-medium transition-all duration-300 hover:shadow-lg hover:from-blue-700 hover:to-indigo-700">` containing `<a href="https://api.naturalproducts.net/latest/docs" target="_blank" rel="noopener noreferrer">`. Move ExternalLink and text inside the `<a>`.

3. Funder logo links (lines 895-909): These are image cards showing logos, NOT buttons. Leave them as `motion.a` -- converting to Button would be semantically incorrect (they contain only an img element, not button text). However, wrap each in Card for consistency: Replace `<motion.a ... className="block bg-white dark:bg-slate-800/60 p-3 rounded-lg shadow-md hover:shadow-lg transition-all duration-300 w-auto h-16 flex items-center justify-center" whileHover={{ y: -5, scale: 1.05 }}>` with `<motion.div whileHover={{ y: -5, scale: 1.05 }} transition={{ duration: 0.2 }}>` wrapping `<Card className="bg-white dark:bg-slate-800/60 p-3 rounded-lg shadow-md hover:shadow-lg transition-all duration-300 py-0 gap-0">` wrapping `<a href={funder.url} target="_blank" rel="noopener noreferrer" className="w-auto h-16 flex items-center justify-center">`. The img stays inside the `<a>`. Import Card from @/components/ui/card at the top (Card import may not exist yet -- check and add if needed).

**Footer.tsx** -- Add imports and migrate 4 resource cards:

1. Add imports: `import { Card } from "@/components/ui/card";`

2. Resource links (lines 350-397): Each `<motion.a ... className="group flex flex-col justify-center items-start p-2 rounded-lg border border-slate-200 dark:border-slate-700 bg-white/50 dark:bg-slate-800/50 backdrop-blur-sm ..." variants={itemVariant} whileHover={{ y: -3, ... }} whileTap={{ scale: 0.98 }}>` should become: `<motion.div variants={itemVariant} whileHover={{ y: -3, boxShadow: "0 10px 25px -5px rgba(59, 130, 246, 0.15)" }} whileTap={{ scale: 0.98 }}>` wrapping `<Card className="group h-full p-0 py-0 gap-0 rounded-lg border-slate-200 dark:border-slate-700 bg-white/50 dark:bg-slate-800/50 backdrop-blur-sm transition-all duration-300 hover:shadow-md hover:border-blue-300 dark:hover:border-blue-500">` wrapping `<a href={link.href} target="_blank" rel="noopener noreferrer" className="flex flex-col justify-center items-start p-2">`. All inner content (gradient accent line, icon+title row, description, animated arrow) stays inside the `<a>`. Note: The inner motion.div (whileHover for icon scale/rotate) and motion.span (animated arrow) stay as-is since they animate independent children, not the card container.

IMPORTANT: Ensure the spread of motion animation props is correct. The whileHover, whileTap, and variants props belong on the motion.div wrapper, NOT on Card or the anchor element.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>AboutPage.tsx has 2 button-styled motion.a elements converted to motion.div + Button asChild + a. Funder logos wrapped in Card. Footer.tsx imports Card. All 4 resource motion.a elements converted to motion.div + Card + a. Zero motion.a elements styled as buttons remain anywhere in the codebase. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full gap closure:

1. **No remaining hand-rolled button patterns:** Run `grep -rn "motion\.a" frontend/src/pages/ frontend/src/components/common/Footer.tsx` -- should return zero results (all motion.a converted to motion.div wrappers).

2. **All pages import shadcn components:** Run `grep -l "from.*@/components/ui/button" frontend/src/pages/*.tsx` -- should list all 9 page files (or at minimum: HomePage, OCSRPage, AboutPage, ChemPage, DepictPage, ConvertPage, ToolsPage).

3. **Build succeeds:** `cd frontend && npx vite build` completes with no errors.

4. **Tests pass:** `cd frontend && npx vitest run --reporter=verbose` -- all existing tests green.
</verification>

<success_criteria>
- Zero `motion.a` elements remain in any page or Footer component
- OCSRPage.tsx imports both Button and Card from @/components/ui/
- HomePage.tsx imports both Button and Card from @/components/ui/
- AboutPage.tsx uses Button asChild for its 2 button-styled external links
- Footer.tsx uses Card for its 4 resource link containers
- All existing motion animations (variants, whileHover, stagger, spring physics) continue working
- `npm run build` succeeds
- All vitest tests pass
- COMP-02, COMP-03, COMP-07 requirements fully satisfied (no partial)
</success_criteria>

<output>
After completion, create `.planning/phases/04-component-system-dark-mode/04-05-SUMMARY.md`
</output>
