---
phase: 03-tailwind-v4-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src/components/chem/AllFiltersView.tsx
  - frontend/src/components/chem/ChemPage.tsx
autonomous: false
requirements:
  - FRAME-04

must_haves:
  truths:
    - "All 9 pages render with correct styling -- no missing colors, broken layouts, or invisible elements"
    - "Dark mode toggling works correctly with no style regressions"
    - "Form inputs display correct borders, focus rings, and placeholder colors"
    - "Cards, buttons, and shadows have correct visual depth"
    - "Gradient backgrounds and text gradients render correctly"
    - "The glass effect utility works in both light and dark modes"
  artifacts:
    - path: "frontend/src/styles/tailwind.css"
      provides: "Fully migrated v4 CSS with zero theme() calls"
      contains: "@import \"tailwindcss\""
    - path: "frontend/src/components/chem/ChemPage.tsx"
      provides: "Fixed transform-none replacement"
      contains: "translate"
  key_links:
    - from: "frontend/src/styles/tailwind.css"
      to: "all .tsx components"
      via: "Tailwind utility classes"
      pattern: "class(Name)?="
    - from: "frontend/src/styles/tailwind.css @theme"
      to: "dark mode variables in .dark block"
      via: "CSS custom properties"
      pattern: "var\\(--color-"
---

<objective>
Audit and fix all edge cases from the Tailwind v4 migration, then visually verify zero regressions across all 9 pages.

Purpose: The upgrade tool handles ~90% of renames automatically, but known edge cases (transform-none removal, ring default changes, border color default, bg-opacity merging, --tw-* variable compatibility) require manual audit. Visual verification is the hard gate -- the migration is only complete when every page looks correct.

Output: Zero visual regressions confirmed, all edge cases resolved, phase complete.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tailwind-v4-migration/03-CONTEXT.md
@.planning/phases/03-tailwind-v4-migration/03-RESEARCH.md
@.planning/phases/03-tailwind-v4-migration/03-01-SUMMARY.md

<interfaces>
<!-- Key edge cases documented in RESEARCH.md that the upgrade tool may NOT handle -->

Pitfall 2 - Border color default: v4 changed default border color from gray-200 to currentColor.
  Should be handled by Plan 01 (base rule added). Verify visually.

Pitfall 3 - bg-opacity merging: `dark:bg-red-900 dark:bg-opacity-30` must become `dark:bg-red-900/30`.
  Upgrade tool should handle. Verify no two-class patterns remain.

Pitfall 5 - Shadow size shift: shadow-sm (v3) = shadow-xs (v4), shadow (v3) = shadow-sm (v4).
  344 shadow usages across 44 files. Verify cards, buttons, modals have correct depth.

Pitfall 6 - Ring default change: `ring` (v3) = 3px blue-500; `ring` (v4) = 1px currentColor.
  Replace bare `ring` with `ring-3 ring-blue-500` where old behavior is desired.

Pitfall 7 - space-between selector change: v4 uses :not(:last-child) instead of :not([hidden]) ~ :not([hidden]).
  Mostly backward-compatible. Visual verification catches issues.

Pitfall 8 - transform-none removed: `md:transform-none` in ChemPage.tsx must be replaced.
  Replace with `md:translate-x-0 md:translate-y-0` or appropriate individual resets.

Pitfall 9 - CSS variable bracket syntax: `bg-[--brand-color]` changed to `bg-(--brand-color)`.
  Codebase uses `bg-[var(--bg-primary)]` format (with explicit var()) which should still work.

Pitfall 10 - --tw-* internal variables: `--tw-ring-color`, `--tw-ring-opacity`, `--tw-gradient-stops`.
  May break in v4. Test and fix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manual edge case audit and fixes</name>
  <files>
    frontend/src/components/chem/ChemPage.tsx,
    frontend/src/components/chem/AllFiltersView.tsx,
    (any TSX files with remaining edge cases)
  </files>
  <action>
    Perform a systematic audit of all known v4 migration edge cases that the upgrade tool may have missed.

    **Audit 1: transform-none removal (ChemPage.tsx)**
    Search for `transform-none` in all TSX files:
    ```bash
    grep -rn "transform-none" frontend/src/
    ```
    The research identified `md:transform-none` in ChemPage.tsx (sidebar reset). Replace with the appropriate individual transform resets. Examine the context to determine which transforms are being reset:
    - If resetting translateX: use `md:translate-x-0`
    - If resetting translateY: use `md:translate-y-0`
    - If resetting scale: use `md:scale-100`
    - If resetting all: use `md:translate-x-0 md:translate-y-0 md:scale-100 md:rotate-0`

    **Audit 2: Bare ring utility**
    Search for bare `ring` class (not ring-2, ring-4, etc.) that expects the v3 default of 3px:
    ```bash
    grep -rn "ring[\" ]" frontend/src/ --include="*.tsx" | grep -v "ring-"
    ```
    Any bare `ring` should be evaluated. If it was used for focus styling expecting 3px blue-500, replace with `ring-3 ring-blue-500` (or appropriate color).

    **Audit 3: bg-opacity two-class patterns**
    Search for any remaining `bg-opacity-` classes that should have been merged by the upgrade tool:
    ```bash
    grep -rn "bg-opacity-" frontend/src/ --include="*.tsx"
    ```
    Each occurrence should have been converted to the slash modifier syntax (e.g., `dark:bg-red-900/30`). Fix any that remain.

    **Audit 4: Arbitrary CSS variable bracket syntax**
    Search for `[--` patterns (without explicit `var()`) that need to become `(--`:
    ```bash
    grep -rn "\[--" frontend/src/ --include="*.tsx"
    ```
    Patterns like `bg-[var(--bg-primary)]` (with explicit `var()`) are fine. Patterns like `bg-[--brand-color]` need to become `bg-(--brand-color)`.

    **Audit 5: prose dark mode**
    Search for `prose` class usage and ensure dark mode works:
    ```bash
    grep -rn "prose" frontend/src/ --include="*.tsx"
    ```
    If `prose` is used with dark mode, ensure `dark:prose-invert` is present alongside. The old custom typography overrides from tailwind.config.js are replaced by the built-in `prose-invert`.

    **Audit 6: --tw-gradient-stops usage**
    The bg-gradient-radial utility in tailwind.css references `var(--tw-gradient-stops)`. Verify this internal variable still works in v4. If not, replace with explicit gradient stops or the v4 approach.

    **Audit 7: Verify AllFiltersView.tsx theme() replacements from Plan 01**
    Confirm both occurrences on lines 756 and 785 now use `var(--spacing-12)` instead of `theme(space.12)`.

    **Audit 8: Final sweep for any remaining v3 patterns**
    ```bash
    grep -rn "bg-gradient-to-" frontend/src/ --include="*.tsx"
    grep -rn "flex-shrink-" frontend/src/ --include="*.tsx"
    grep -rn "flex-grow[\" ]" frontend/src/ --include="*.tsx"
    grep -rn "outline-none" frontend/src/ --include="*.tsx"
    ```
    These should all have been renamed by the upgrade tool. Fix any stragglers.

    **Build and test after all fixes:**
    ```bash
    cd frontend && npx vite build && npx vitest run
    ```
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && grep -rn "transform-none\|bg-opacity-\|flex-shrink-\|bg-gradient-to-" src/ --include="*.tsx" | head -5; echo "---"; npx vite build 2>&1 | tail -3</automated>
  </verify>
  <done>
    - No `transform-none` in any TSX file
    - No `bg-opacity-*` two-class patterns remain (all converted to slash modifier)
    - No v3 class names remain (bg-gradient-to-*, flex-shrink-*, outline-none)
    - Bare `ring` usages evaluated and fixed if needed
    - Arbitrary CSS variable bracket syntax checked
    - Prose dark mode uses prose-invert
    - Build succeeds, tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 9 pages</name>
  <action>Human verifies zero visual regressions across all 9 pages in both light and dark mode after complete Tailwind v3-to-v4 migration.</action>
  <what-built>
    Complete Tailwind CSS v3-to-v4 migration:
    - Tailwind v4 with @tailwindcss/vite plugin (no PostCSS)
    - CSS-first config (@import, @theme, @plugin, @custom-variant)
    - All 32 theme() calls replaced with CSS variables
    - Class renames across all TSX files (gradients, shadows, opacity, flex, outline, rounded, blur)
    - Custom utilities (glass, text-gradient) using @utility directive
    - Animation consolidation (bounce-slow deduplication)
    - Edge cases fixed (transform-none, border defaults, ring defaults)
    - Unused patterns and shadows removed
  </what-built>
  <how-to-verify>
    Start the dev server:
    ```bash
    cd frontend && npm run dev
    ```
    Visit http://localhost:3000 and systematically check ALL 9 pages:

    **For each page, verify:**
    1. Layout: correct grid/flex structure, no broken positioning, sidebar/mobile behavior
    2. Colors: correct background, text, accent colors in both light and dark mode
    3. Borders: correct color and width (watch for currentColor default change)
    4. Shadows: cards and containers have correct depth (watch for shadow size shift)
    5. Forms: inputs have correct background, border, focus ring, placeholder color
    6. Buttons: correct hover/focus states, consistent styling
    7. Typography: prose content (if any) renders correctly
    8. Glass effects: semi-transparent with backdrop blur
    9. Gradients: text-gradient, background gradients render correctly
    10. Animations: pulse-slow, pulse-fast, bounce-slow work

    **Pages to check (all 9):**
    1. Home / Landing page
    2. Chem tools page (the main tool page with sidebar)
    3. Depict page (structure depiction)
    4. Convert page (format conversion)
    5. Tools page (utility tools)
    6. About page
    7. Any remaining pages from the route structure

    **Dark mode test:**
    - Toggle dark mode (if toggle exists) or manually add `dark` class to `<html>` element in DevTools
    - Re-check colors, backgrounds, borders, form inputs, cards in dark mode
    - Verify glass effect changes appearance in dark mode

    **Specific regression checks:**
    - Cards should have visible shadows (not flatter than before)
    - Form input focus should show a colored ring (not invisible)
    - Sidebar on ChemPage should reset position correctly on medium+ screens
    - AllFiltersView sticky columns should be positioned correctly
  </how-to-verify>
  <resume-signal>Type "approved" if all 9 pages look correct, or describe any visual issues found</resume-signal>
</task>

</tasks>

<verification>
1. Zero v3 class names remain: `grep -rn "bg-gradient-to-\|flex-shrink-\|outline-none\|transform-none\|bg-opacity-" frontend/src/ --include="*.tsx"` returns empty
2. Zero theme() calls remain: `grep -rn "theme(" frontend/src/` returns empty
3. Build succeeds: `cd frontend && npx vite build` exits 0
4. Tests pass: `cd frontend && npx vitest run` exits 0
5. No legacy config files: `test ! -f frontend/tailwind.config.js && test ! -f frontend/postcss.config.js`
6. Human verified all 9 pages with zero visual regressions
</verification>

<success_criteria>
- All v4 migration edge cases resolved (transform-none, ring defaults, border defaults, bg-opacity merging)
- No v3 class names remain anywhere in the codebase
- No theme() function calls remain anywhere
- Production build succeeds
- All existing tests pass
- Human confirms all 9 pages render correctly with zero visual regressions
- Dark mode styling works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-tailwind-v4-migration/03-02-SUMMARY.md`
</output>
