---
phase: 04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/styles/tailwind.css
  - frontend/src/config/gradients.ts
  - frontend/src/config/bentoLayouts.ts
  - frontend/src/hooks/useMediaQuery.ts
  - frontend/src/components/common/GradientMesh.tsx
  - frontend/src/components/common/LiquidGlassFilter.tsx
autonomous: true
requirements: [VISUAL-01, VISUAL-06]
must_haves:
  truths:
    - "Glass utility classes (glass-bold, glass-mobile) apply correct backdrop-filter and background values in both light and dark modes"
    - "Clay utility classes (clay-interactive, clay-pressed) produce raised/pressed shadow effects in both modes"
    - "Per-page gradient mesh backgrounds render with correct color palettes (blue-purple for Home, sky-teal for Chem, etc.)"
    - "SVG liquid glass refraction filter exists and is applied on hover (desktop only)"
    - "Mobile viewports get reduced blur (12px) and simplified gradients"
  artifacts:
    - path: "frontend/src/styles/tailwind.css"
      provides: "glass-bold, glass-mobile, clay-interactive, clay-pressed, glass-refraction-hover utilities"
      contains: "@utility glass-bold"
    - path: "frontend/src/config/gradients.ts"
      provides: "PAGE_GRADIENTS config with light/dark/mobile variants for all 9 pages"
      exports: ["PAGE_GRADIENTS"]
    - path: "frontend/src/config/bentoLayouts.ts"
      provides: "Bento grid sizing config per page (hero/medium/compact tool assignments)"
      exports: ["BENTO_LAYOUTS"]
    - path: "frontend/src/hooks/useMediaQuery.ts"
      provides: "Responsive breakpoint detection hook"
      exports: ["useMediaQuery"]
    - path: "frontend/src/components/common/GradientMesh.tsx"
      provides: "Reusable gradient mesh background component with per-page palettes"
      exports: ["GradientMesh"]
    - path: "frontend/src/components/common/LiquidGlassFilter.tsx"
      provides: "Hidden SVG filter definition for liquid glass refraction"
      exports: ["LiquidGlassFilter"]
  key_links:
    - from: "frontend/src/styles/tailwind.css"
      to: "All page and component files"
      via: "className usage of glass-bold, clay-interactive etc."
      pattern: "glass-bold|clay-interactive"
    - from: "frontend/src/config/gradients.ts"
      to: "frontend/src/components/common/GradientMesh.tsx"
      via: "import PAGE_GRADIENTS"
      pattern: "PAGE_GRADIENTS"
---

<objective>
Create the visual design system foundation: CSS utilities for glassmorphism and claymorphism, per-page gradient mesh configurations, SVG liquid glass refraction filter, responsive media query hook, and bento grid layout configuration.

Purpose: Everything in this phase depends on these foundational utilities and configurations. Glass utilities are used by every page, gradient meshes provide the visual atmosphere, and the bento layout config determines how tools are sized across pages.

Output: CSS utilities in tailwind.css, gradient config, bento layout config, useMediaQuery hook, GradientMesh component, LiquidGlassFilter SVG component.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-CONTEXT.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-RESEARCH.md

@frontend/src/styles/tailwind.css
@frontend/src/hooks/useMediaQuery.ts (to be created)
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS utility system -- glass-bold, glass-mobile, clay-interactive, clay-pressed, refraction hover</name>
  <files>frontend/src/styles/tailwind.css</files>
  <action>
Update tailwind.css to add new @utility directives. Keep ALL existing content (OKLCH variables, theme, base styles, existing utilities). Add after the existing `@utility glass` block:

1. **glass-bold** utility (the primary glass surface per user decision):
   - Light: `background-color: rgb(255 255 255 / 0.1)`, `backdrop-filter: blur(24px)`, `-webkit-backdrop-filter: blur(24px)`, `border: 1px solid rgb(255 255 255 / 0.3)`, `box-shadow: inset 0 1px 1px 0 rgb(255 255 255 / 0.15)`
   - Dark (via `&:where(.dark, .dark *)`): `background-color: rgb(30 41 59 / 0.2)`, border `rgb(148 163 184 / 0.15)`, inner shadow `rgb(255 255 255 / 0.05)`

2. **glass-mobile** utility (reduced blur for GPU perf on mobile):
   - Light: `background-color: rgb(255 255 255 / 0.1)`, `backdrop-filter: blur(12px)`, `-webkit-backdrop-filter: blur(12px)`, `border: 1px solid rgb(255 255 255 / 0.2)`
   - Dark: `background-color: rgb(30 41 59 / 0.2)`, border `rgb(148 163 184 / 0.1)`

3. **clay-interactive** utility (raised interactive elements -- buttons, toggles, cards):
   - Light: `border-radius: 16px`, box-shadow with inner bottom shadow `rgb(0 0 0 / 0.06)`, inner top highlight `rgb(255 255 255 / 0.4)`, outer drop `rgb(0 0 0 / 0.12)`, ambient `rgb(0 0 0 / 0.04)`
   - Dark: increase shadow darkness, reduce highlight intensity (white/8% not white/40%)

4. **clay-pressed** utility (pressed/active state):
   - Light: inverted inner shadows creating pressed-in appearance
   - Dark: adjusted for dark backgrounds

5. Add a `.glass-refraction-hover` rule in a `@layer components` block (not @utility since it uses :hover pseudo and needs `@media (hover: hover)` wrapping):
   ```css
   @media (hover: hover) {
     .glass-refraction-hover:hover {
       filter: url(#liquid-glass-refraction);
       transform: scale(1.005);
       transition: filter 300ms ease, transform 300ms ease;
     }
   }
   ```
   Also add the un-hovered transition reset:
   ```css
   .glass-refraction-hover {
     transition: filter 300ms ease, transform 300ms ease;
   }
   ```

Use exact values from RESEARCH.md Pattern 1. Ensure -webkit-backdrop-filter prefix is included for Safari.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>tailwind.css contains glass-bold, glass-mobile, clay-interactive, clay-pressed utilities and glass-refraction-hover rule. Build succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Gradient mesh config, bento layout config, useMediaQuery hook, GradientMesh component, LiquidGlassFilter SVG</name>
  <files>frontend/src/config/gradients.ts, frontend/src/config/bentoLayouts.ts, frontend/src/hooks/useMediaQuery.ts, frontend/src/components/common/GradientMesh.tsx, frontend/src/components/common/LiquidGlassFilter.tsx</files>
  <action>
Create `frontend/src/config/` directory if it does not exist.

**1. `frontend/src/config/gradients.ts`** -- Per-page gradient mesh definitions:
Export `PAGE_GRADIENTS` as a const object with keys: `home`, `chem`, `convert`, `depict`, `ocsr`, `tools`, `about`, `privacy`, `terms`. Each key has `light` (3 stacked radial-gradients joined by comma), `dark` (jewel-toned equivalents), `mobileLight` (simple linear-gradient), `mobileDark` (simple linear-gradient).

Color palettes per user decision:
- home: blue->purple (rgb 147,197,253 / 196,181,253 / 165,180,252 light; rgb 30,64,175 / 88,28,135 / 49,46,129 dark)
- chem: sky->teal (rgb 125,211,252 / 94,234,212 / 186,230,253 light; rgb 14,116,144 / 13,148,136 / 8,145,178 dark)
- convert: indigo->blue (rgb 165,180,252 / 147,197,253 / 129,140,248 light; rgb 49,46,129 / 30,64,175 / 67,56,202 dark)
- depict: violet->pink (rgb 196,181,253 / 249,168,212 / 216,180,254 light; rgb 88,28,135 / 157,23,77 / 109,40,217 dark)
- ocsr: emerald->cyan (rgb 110,231,183 / 103,232,249 / 167,243,208 light; rgb 4,120,87 / 14,116,144 / 6,95,70 dark)
- tools: amber->orange (rgb 253,230,138 / 253,186,116 / 254,215,170 light; rgb 146,64,14 / 154,52,18 / 120,53,15 dark)
- about: slate->blue (rgb 148,163,184 / 147,197,253 / 165,180,252 light; rgb 51,65,85 / 30,64,175 / 49,46,129 dark)
- privacy, terms: neutral gray (rgb 148,163,184 / 156,163,175 / 163,163,163 light; rgb 51,65,85 / 55,65,81 / 63,63,70 dark)

Follow RESEARCH.md Example 1 pattern exactly. Use `as const` for type safety.

**2. `frontend/src/config/bentoLayouts.ts`** -- Bento grid sizing config:
Export `BENTO_LAYOUTS` with keys: `chem`, `tools`, `convert`, `depict`, `home`. Each is an array of `{ id: string; size: 'hero' | 'medium' | 'compact' }`. Use sizing recommendations from RESEARCH.md Example 4:

ChemPage: hero = [descriptors, structure-finder], medium = [stereoisomers, standardize, similarity, classyfire], compact = rest
ToolsPage: hero = [sugardetection, structuregeneration], medium = [inchiconverter, rinchiconverter]
ConvertPage: hero = [format-conversion], medium = [2d-coordinates, 3d-coordinates]
DepictPage: hero = [structure-explorer], medium = [2d-depiction, 3d-depiction], compact = [draw-structure]
HomePage: All features are medium size (editorial grid, no hero distinction needed for feature links)

Also export a `BentoSize` type and a `getBentoSize(pageKey, toolId)` helper function.

**3. `frontend/src/hooks/useMediaQuery.ts`**:
Export `useMediaQuery(query: string): boolean` hook per RESEARCH.md Example 2. Uses `window.matchMedia`, updates on change events. Initialize with `useState(false)` and set in useEffect to avoid SSR mismatch (though this is a SPA).

**4. `frontend/src/components/common/GradientMesh.tsx`**:
Export `GradientMesh` component. Props: `page: keyof typeof PAGE_GRADIENTS`. Renders an absolutely-positioned div with `inset-0 -z-20` containing the gradient mesh background. Uses `useAppContext()` to detect isDarkMode and `useMediaQuery('(max-width: 767px)')` for mobile variant. Applies the correct gradient from PAGE_GRADIENTS based on dark mode and mobile state.

**5. `frontend/src/components/common/LiquidGlassFilter.tsx`**:
Export `LiquidGlassFilter` component. Renders a hidden SVG (`width="0" height="0" style={{ position: 'absolute' }}`) with a `<defs>` block containing the `<filter id="liquid-glass-refraction">` using feTurbulence (baseFrequency="0.015", numOctaves="3", seed="2") and feDisplacementMap (scale="6", xChannelSelector="R", yChannelSelector="G"). Per RESEARCH.md Pattern 5.

This component should be rendered once in the app layout (will be added in a later plan when pages are updated).
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 files created and exported. `npm run build` succeeds. PAGE_GRADIENTS covers all 9 pages. BENTO_LAYOUTS covers chem/tools/convert/depict/home. useMediaQuery hook works. GradientMesh renders gradient background. LiquidGlassFilter renders hidden SVG filter.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run --reporter=verbose` -- all existing tests pass
- `cd frontend && npm run build` -- production build succeeds
- tailwind.css contains glass-bold, glass-mobile, clay-interactive, clay-pressed @utility blocks
- gradients.ts exports PAGE_GRADIENTS with 9 page keys
- bentoLayouts.ts exports BENTO_LAYOUTS with 5 page keys
- useMediaQuery.ts exports useMediaQuery hook
- GradientMesh.tsx exports GradientMesh component
- LiquidGlassFilter.tsx exports LiquidGlassFilter component
</verification>

<success_criteria>
- All new CSS utilities are defined in tailwind.css and the build compiles without errors
- Gradient mesh configuration covers all 9 pages with light/dark/mobile variants
- Bento layout configuration assigns hero/medium/compact sizes to all tools
- useMediaQuery hook detects viewport breakpoints
- Foundation components (GradientMesh, LiquidGlassFilter) are ready for integration
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-01-SUMMARY.md`
</output>
