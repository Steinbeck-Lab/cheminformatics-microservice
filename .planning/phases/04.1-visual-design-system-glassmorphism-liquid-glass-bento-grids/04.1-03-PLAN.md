---
phase: 04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids
plan: 03
type: execute
wave: 3
depends_on: [04.1-02]
files_modified:
  - frontend/src/pages/ConvertPage.tsx
  - frontend/src/pages/DepictPage.tsx
  - frontend/src/pages/ToolsPage.tsx
  - frontend/src/pages/HomePage.tsx
autonomous: true
requirements: [VISUAL-02]
must_haves:
  truths:
    - "ConvertPage shows 3 tools in bento grid layout (hero Format Conversion, medium 2D/3D Coordinates)"
    - "DepictPage shows 4 tools in bento grid layout (hero Structure Explorer, medium 2D/3D Depiction, compact Draw)"
    - "ToolsPage shows 4 tools in bento grid layout (hero Sugar Detection/Structure Gen, medium InChI/RInChI)"
    - "HomePage feature cards use bento grid layout with editorial sizing"
    - "All 4 pages have gradient mesh backgrounds matching their per-page color palettes"
    - "Expand/collapse works identically to ChemPage on all bento pages"
  artifacts:
    - path: "frontend/src/pages/ConvertPage.tsx"
      provides: "ConvertPage refactored to bento grid"
      contains: "BentoGrid"
    - path: "frontend/src/pages/DepictPage.tsx"
      provides: "DepictPage refactored to bento grid"
      contains: "BentoGrid"
    - path: "frontend/src/pages/ToolsPage.tsx"
      provides: "ToolsPage refactored to bento grid"
      contains: "BentoGrid"
    - path: "frontend/src/pages/HomePage.tsx"
      provides: "HomePage with glass hero and bento feature cards"
      contains: "GradientMesh"
  key_links:
    - from: "frontend/src/pages/ConvertPage.tsx"
      to: "frontend/src/components/common/BentoGrid.tsx"
      via: "import BentoGrid, BentoCard"
      pattern: "BentoGrid"
    - from: "frontend/src/pages/DepictPage.tsx"
      to: "frontend/src/components/common/BentoGrid.tsx"
      via: "import BentoGrid, BentoCard"
      pattern: "BentoGrid"
    - from: "frontend/src/pages/ToolsPage.tsx"
      to: "frontend/src/components/common/BentoGrid.tsx"
      via: "import BentoGrid, BentoCard"
      pattern: "BentoGrid"
---

<objective>
Migrate ConvertPage, DepictPage, and ToolsPage from tab navigation to bento grid layout, and redesign HomePage with glass hero section and bento-style feature cards.

Purpose: Complete the bento grid rollout across all tab-based pages and the homepage. After this plan, all 5 pages that had tabs (or feature cards) use the new bento grid pattern.

Output: 4 refactored page files using BentoGrid, BentoCard, GradientMesh components.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-CONTEXT.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-RESEARCH.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-02-SUMMARY.md

@frontend/src/pages/ConvertPage.tsx
@frontend/src/pages/DepictPage.tsx
@frontend/src/pages/ToolsPage.tsx
@frontend/src/pages/HomePage.tsx
@frontend/src/components/common/BentoGrid.tsx

<interfaces>
<!-- BentoGrid and BentoCard from Plan 02 -->
From frontend/src/components/common/BentoGrid.tsx:
```typescript
interface BentoCardProps {
  id: string;
  pageKey: string;
  name: string;
  description: string;
  icon: React.ComponentType<{ className?: string }>;
  size: BentoSize;
  isExpanded: boolean;
  isOtherExpanded: boolean;
  onToggle: () => void;
  children?: React.ReactNode;
}
export function BentoGrid({ pageKey, children, expandedId }: { pageKey: string; children: React.ReactNode; expandedId: string | null }): JSX.Element;
export function BentoCard(props: BentoCardProps): JSX.Element;
```

From frontend/src/config/bentoLayouts.ts:
```typescript
export function getBentoSize(pageKey: string, toolId: string): BentoSize;
```

From frontend/src/components/common/GradientMesh.tsx:
```typescript
export function GradientMesh({ page }: { page: string }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ConvertPage, DepictPage, ToolsPage to bento grid</name>
  <files>frontend/src/pages/ConvertPage.tsx, frontend/src/pages/DepictPage.tsx, frontend/src/pages/ToolsPage.tsx</files>
  <action>
Apply the same refactoring pattern established on ChemPage (Plan 02) to these 3 pages. Each page follows the identical structure:

**For each page (ConvertPage, DepictPage, ToolsPage):**

**Keep:**
- All tool component imports (FormatConversionView, Mol2DView, etc.)
- All lucide icon imports
- The `tabs` array definition (id, name, description, icon, component)
- pageVariants animation variant

**Remove:**
- Tab bar rendering code (the horizontal tab buttons with LayoutGroup animated indicators)
- Tab-switching state management (`activeTab`, `setActiveTab`)
- headerContainerVariants, headerItemVariants, tabContainerVariant, contentVariants animation variants
- The `ChevronDown` icon import (used in mobile tab dropdown -- no longer needed)
- Mobile tab dropdown select
- `useNavigate` import if only used for tabs (check if still needed for URL params)

**Add:**
- Import `BentoGrid, BentoCard` from `@/components/common/BentoGrid`
- Import `getBentoSize` from `@/config/bentoLayouts`
- Import `GradientMesh` from `@/components/common/GradientMesh`
- State: `const [expandedToolId, setExpandedToolId] = useState<string | null>(null)`
- URL param support with useParams/useNavigate (same pattern as ChemPage)

**ConvertPage specifics:**
- pageKey="convert", GradientMesh page="convert"
- 3 tools: format-conversion (hero), 2d-coordinates (medium), 3d-coordinates (medium)
- Route: `/convert` and `/convert/:toolName`

**DepictPage specifics:**
- pageKey="depict", GradientMesh page="depict"
- 4 tools based on existing tabs array (read DepictPage.tsx to get exact tool IDs)
- Route: `/depict` and `/depict/:toolName`

**ToolsPage specifics:**
- pageKey="tools", GradientMesh page="tools"
- 4 tools: sugardetection (hero), structuregeneration (hero), inchiconverter (medium), rinchiconverter (medium)
- Route: `/tools` and `/tools/:toolName`

Each page follows this structure:
```tsx
<motion.div variants={pageVariants} initial="hidden" animate="visible" className="relative min-h-screen">
  <GradientMesh page="{pageKey}" />
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <h1 ...>{Page Title}</h1>
    <p ...>{Page description}</p>
  </div>
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-12">
    <BentoGrid pageKey="{pageKey}" expandedId={expandedToolId}>
      {tabs.map(tab => <BentoCard ... />)}
    </BentoGrid>
  </div>
</motion.div>
```

Verify each page's existing `useParams` and `useNavigate` usage to ensure URL routing is preserved.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>ConvertPage, DepictPage, and ToolsPage all render bento grids instead of tab bars. Each page has its own gradient mesh background. URL routing preserved. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign HomePage with glass hero and bento feature cards</name>
  <files>frontend/src/pages/HomePage.tsx</files>
  <action>
Refactor HomePage.tsx to use the glassmorphism aesthetic and bento-style feature cards.

**Keep:**
- All imports (React, Link, motion, useAppContext, MoleculeCard, lucide icons, Button, Card)
- The `features` array definition
- The recent molecules section (MoleculeCard rendering)
- containerVariants, itemVariants, heroTextVariants, sectionFadeIn animation variants
- TiltCard helper component

**Modify:**

1. **Add gradient mesh background:**
   - Import GradientMesh from `@/components/common/GradientMesh`
   - Add `<GradientMesh page="home" />` as first child of the page wrapper

2. **Glass hero section:**
   - Wrap the hero content (title, tagline, CTA button) in a `glass-bold rounded-3xl` container
   - Per user decision: "Glass hero card: large frosted glass panel over page gradient containing title, tagline, and CTA button"
   - CTA button gets `clay-interactive` class for raised tactile feel
   - The existing gradient text effect (text-gradient) should be preserved

3. **Bento-style feature cards:**
   - Replace the current uniform grid of feature cards with a bento-style varied layout
   - Use CSS Grid with varied sizing: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`
   - Feature cards (Chemical Analysis, Format Conversion, Visualization, Structure Tools, OCSR) use `glass-bold rounded-2xl` styling
   - Each feature card contains its lucide icon, title, description, and a "Learn more" link
   - Cards are NOT expandable (they navigate to their page via Link) -- this is different from tool pages
   - Apply `clay-interactive` class to feature cards for raised interactive feel
   - Add hover lift effect: `hover:-translate-y-1 transition-transform duration-200`

4. **Recent molecules section:**
   - Wrap in a glass-bold container
   - Keep existing MoleculeCard rendering

5. **Remove/simplify:**
   - Remove ribbonContentVariants if unused after refactor
   - The TiltCard component can be kept for feature cards if it adds to the tactile feel, or removed if it conflicts with glass styling (use judgment)

Do NOT add expand/collapse to HomePage feature cards -- they are navigation links, not tool containers.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>HomePage has gradient mesh background, glass hero section with clay CTA button, bento-style feature cards with glass-bold styling, and glass-wrapped recent molecules section. Build succeeds.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` -- production build succeeds
- `cd frontend && npx vitest run --reporter=verbose` -- existing tests pass
- All 4 pages import BentoGrid or GradientMesh
- No tab-bar code remains in ConvertPage, DepictPage, ToolsPage
- HomePage has glass hero section
- URL routing works for all tool pages
</verification>

<success_criteria>
- All 4 tab-based tool pages (Chem, Convert, Depict, Tools) use bento grid layout
- HomePage has glass hero section and bento-style feature cards
- All 5 pages have per-page gradient mesh backgrounds
- Expand/collapse works on all tool bento pages
- Feature cards on HomePage navigate to tool pages (no expand)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-03-SUMMARY.md`
</output>
