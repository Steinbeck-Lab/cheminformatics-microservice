---
phase: 04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/__tests__/styles/glass-utilities.test.ts
  - frontend/src/__tests__/styles/clay-utilities.test.ts
  - frontend/src/__tests__/components/bento-grid.test.tsx
  - frontend/src/__tests__/components/molecule-card-enhanced.test.tsx
  - frontend/src/__tests__/components/resizable-tool-panel.test.tsx
  - frontend/src/__tests__/context/comparison.test.tsx
autonomous: true
requirements: [VISUAL-01, VISUAL-02, VISUAL-03, VISUAL-04, VISUAL-05, VISUAL-06]
must_haves:
  truths:
    - "All 6 test stub files exist and are runnable by vitest"
    - "Each test file contains describe blocks with placeholder tests that skip or pass trivially"
    - "Running vitest on the test files does not error (imports resolve, mocks work)"
  artifacts:
    - path: "frontend/src/__tests__/styles/glass-utilities.test.ts"
      provides: "Test stubs for VISUAL-01 glass utility classes"
      contains: "glass-bold"
    - path: "frontend/src/__tests__/styles/clay-utilities.test.ts"
      provides: "Test stubs for VISUAL-06 clay utility classes"
      contains: "clay-interactive"
    - path: "frontend/src/__tests__/components/bento-grid.test.tsx"
      provides: "Test stubs for VISUAL-02 bento grid rendering and expand/collapse"
      contains: "BentoGrid"
    - path: "frontend/src/__tests__/components/molecule-card-enhanced.test.tsx"
      provides: "Test stubs for VISUAL-03 molecule card hover glow and expand"
      contains: "MoleculeCard"
    - path: "frontend/src/__tests__/components/resizable-tool-panel.test.tsx"
      provides: "Test stubs for VISUAL-04 resizable panel responsive behavior"
      contains: "ResizableToolPanel"
    - path: "frontend/src/__tests__/context/comparison.test.tsx"
      provides: "Test stubs for VISUAL-05 comparison context state management"
      contains: "useComparison"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all 6 VISUAL requirements so that subsequent plans can reference concrete automated verification commands rather than relying solely on `npm run build`.

Purpose: VALIDATION.md specifies 6 unit test files that all plans should verify against. Without these stubs, plans have no behavioral regression checks beyond build success. These stubs will be fleshed out as each plan creates the corresponding production code.

Output: 6 test stub files in `frontend/src/__tests__/` ready for vitest.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-CONTEXT.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-VALIDATION.md

@frontend/src/__tests__/setup.ts
@frontend/vite.config.mts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for styles (VISUAL-01, VISUAL-06)</name>
  <files>frontend/src/__tests__/styles/glass-utilities.test.ts, frontend/src/__tests__/styles/clay-utilities.test.ts</files>
  <action>
Create `frontend/src/__tests__/styles/` directory if it does not exist.

**1. `glass-utilities.test.ts`** (VISUAL-01):
```typescript
import { describe, it, expect } from 'vitest';

describe('Glass utility classes', () => {
  describe('glass-bold', () => {
    it.todo('applies backdrop-filter blur(24px) in light mode');
    it.todo('applies backdrop-filter blur(24px) in dark mode');
    it.todo('applies background-color with white/10 transparency');
    it.todo('includes -webkit-backdrop-filter for Safari');
    it.todo('includes border with white/30 transparency');
  });

  describe('glass-mobile', () => {
    it.todo('applies reduced backdrop-filter blur(12px)');
    it.todo('applies background-color with white/10 transparency');
  });

  describe('glass-refraction-hover', () => {
    it.todo('only activates on hover-capable devices');
    it.todo('applies SVG filter url on hover');
    it.todo('applies subtle scale(1.005) on hover');
  });
});
```

**2. `clay-utilities.test.ts`** (VISUAL-06):
```typescript
import { describe, it, expect } from 'vitest';

describe('Clay utility classes', () => {
  describe('clay-interactive', () => {
    it.todo('applies border-radius 16px');
    it.todo('applies inner bottom shadow and outer drop shadow in light mode');
    it.todo('applies darker shadows in dark mode');
  });

  describe('clay-pressed', () => {
    it.todo('applies inverted inner shadows for pressed appearance');
    it.todo('adjusts shadows for dark mode');
  });
});
```

These are `.todo` tests -- they document expected behavior without failing. They will be fleshed out once Plan 01 creates the actual CSS utilities. The `.todo` approach means vitest reports them as skipped, not failed.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npx vitest run src/__tests__/styles/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist, vitest runs them without errors, .todo tests show as skipped.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for components and context (VISUAL-02 through VISUAL-05)</name>
  <files>frontend/src/__tests__/components/bento-grid.test.tsx, frontend/src/__tests__/components/molecule-card-enhanced.test.tsx, frontend/src/__tests__/components/resizable-tool-panel.test.tsx, frontend/src/__tests__/context/comparison.test.tsx</files>
  <action>
Create `frontend/src/__tests__/components/` and `frontend/src/__tests__/context/` directories if they do not exist.

**1. `bento-grid.test.tsx`** (VISUAL-02):
```tsx
import { describe, it, expect } from 'vitest';

describe('BentoGrid', () => {
  it.todo('renders a CSS grid container with correct column classes');
  it.todo('switches to single column when expandedId is set');
  it.todo('wraps children in LayoutGroup');
});

describe('BentoCard', () => {
  it.todo('renders card with name, description, and icon');
  it.todo('applies hero size classes (col-span-2 row-span-2) on lg screens');
  it.todo('applies medium size classes (col-span-2) on sm screens');
  it.todo('applies compact size classes (col-span-1)');
  it.todo('calls onToggle when clicked');
  it.todo('renders children when isExpanded is true');
  it.todo('applies opacity 0.3 and scale 0.95 when isOtherExpanded');
  it.todo('shows close button when expanded');
});
```

**2. `molecule-card-enhanced.test.tsx`** (VISUAL-03):
```tsx
import { describe, it, expect } from 'vitest';

describe('MoleculeCard enhanced', () => {
  it.todo('renders with glass-bold class');
  it.todo('renders with clay-interactive class');
  it.todo('has hover translate and shadow classes');
  it.todo('renders expand/collapse toggle when isExpandable is true');
  it.todo('shows additional details when expanded');
  it.todo('hides additional details when collapsed');
});
```

**3. `resizable-tool-panel.test.tsx`** (VISUAL-04):
```tsx
import { describe, it, expect } from 'vitest';

describe('ResizableToolPanel', () => {
  it.todo('renders horizontal split layout on wide screens (>1024px)');
  it.todo('renders stacked vertical layout on narrow screens (<1024px)');
  it.todo('renders inputContent in left/top panel');
  it.todo('renders outputContent in right/bottom panel');
  it.todo('uses autoSaveId for localStorage persistence');
});
```

**4. `comparison.test.tsx`** (VISUAL-05):
```tsx
import { describe, it, expect } from 'vitest';

describe('ComparisonContext', () => {
  it.todo('starts with empty molecules array');
  it.todo('addMolecule adds a molecule');
  it.todo('addMolecule rejects duplicate SMILES');
  it.todo('addMolecule rejects when already at 2 molecules');
  it.todo('canAdd returns false when 2 molecules present');
  it.todo('removeMolecule removes by SMILES');
  it.todo('clearAll resets to empty state');
  it.todo('auto-opens tray on first molecule add');
  it.todo('closes tray when last molecule removed');
});

describe('ComparisonTray', () => {
  it.todo('renders with glass-bold class');
  it.todo('shows molecule titles when molecules present');
  it.todo('disables Compare button when fewer than 2 molecules');
  it.todo('enables Compare button when 2 molecules present');
});
```

All tests use `.todo` so they document expected behavior without failing. They will be converted to real assertions as each plan creates the production code.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npx vitest run src/__tests__/components/ src/__tests__/context/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All 4 test stub files exist, vitest runs them without errors, .todo tests show as skipped. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run src/__tests__/styles/ src/__tests__/components/ src/__tests__/context/ --reporter=verbose` -- all stubs run, todos reported as skipped
- `cd frontend && npx vitest run --reporter=verbose` -- full test suite still passes
- All 6 files exist at the paths specified in VALIDATION.md
</verification>

<success_criteria>
- All 6 test stub files exist and are recognized by vitest
- Running vitest on these files produces no errors (todos are skipped, not failed)
- Full existing test suite remains green
- VALIDATION.md Wave 0 requirements are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-00-SUMMARY.md`
</output>
