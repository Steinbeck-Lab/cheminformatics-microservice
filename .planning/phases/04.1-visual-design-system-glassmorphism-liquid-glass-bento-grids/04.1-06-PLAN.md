---
phase: 04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids
plan: 06
type: execute
wave: 5
depends_on: [04.1-03, 04.1-04, 04.1-05]
files_modified:
  - frontend/src/components/common/Header.tsx
  - frontend/src/components/common/Footer.tsx
  - frontend/src/pages/OCSRPage.tsx
  - frontend/src/pages/AboutPage.tsx
  - frontend/src/pages/PrivacyPolicy.tsx
  - frontend/src/pages/TermsOfService.tsx
autonomous: false
requirements: [VISUAL-01, VISUAL-06]
must_haves:
  truths:
    - "Header uses frosted glass bar styling with backdrop-blur, content scrolls behind it"
    - "Footer matches header glass aesthetic"
    - "OCSRPage has emerald-cyan gradient mesh and glass containers"
    - "AboutPage has slate-blue gradient mesh and glass containers"
    - "PrivacyPolicy and TermsOfService have neutral gray gradient mesh and glass containers"
    - "All pages have glass containers around content sections"
    - "Claymorphism accents on interactive elements (buttons, links) across all pages"
    - "Both light and dark modes look correct with same glass intensity"
  artifacts:
    - path: "frontend/src/components/common/Header.tsx"
      provides: "Frosted glass header bar"
      contains: "glass-bold"
    - path: "frontend/src/components/common/Footer.tsx"
      provides: "Frosted glass footer"
      contains: "glass-bold"
    - path: "frontend/src/pages/OCSRPage.tsx"
      provides: "OCSRPage with glass + gradient mesh"
      contains: "GradientMesh"
    - path: "frontend/src/pages/AboutPage.tsx"
      provides: "AboutPage with glass + gradient mesh"
      contains: "GradientMesh"
    - path: "frontend/src/pages/PrivacyPolicy.tsx"
      provides: "PrivacyPolicy with glass + gradient mesh"
      contains: "GradientMesh"
    - path: "frontend/src/pages/TermsOfService.tsx"
      provides: "TermsOfService with glass + gradient mesh"
      contains: "GradientMesh"
  key_links:
    - from: "frontend/src/components/common/Header.tsx"
      to: "frontend/src/styles/tailwind.css"
      via: "glass-bold class usage"
      pattern: "glass-bold"
    - from: "All 4 remaining pages"
      to: "frontend/src/components/common/GradientMesh.tsx"
      via: "import GradientMesh for background"
      pattern: "GradientMesh"
---

<objective>
Apply glass treatment to Header and Footer, add gradient mesh backgrounds and glass containers to the 4 remaining pages (OCSR, About, Privacy, Terms), apply claymorphism accents to interactive elements across all updated files, and perform final visual verification.

Purpose: This is the final sweep that ensures ALL 9 pages have the glassmorphism aesthetic. The header and footer are global components seen on every page, so their glass treatment is critical for cohesion. The 4 remaining pages need gradient mesh backgrounds and glass container wrapping.

Output: Glass-styled header and footer, 4 pages with gradient mesh + glass containers, visual verification.
</objective>

<execution_context>
@/Users/kohulanrajan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kohulanrajan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-CONTEXT.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-RESEARCH.md
@.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-01-SUMMARY.md

@frontend/src/components/common/Header.tsx
@frontend/src/components/common/Footer.tsx
@frontend/src/pages/OCSRPage.tsx
@frontend/src/pages/AboutPage.tsx
@frontend/src/pages/PrivacyPolicy.tsx
@frontend/src/pages/TermsOfService.tsx

<interfaces>
From frontend/src/components/common/GradientMesh.tsx:
```typescript
export function GradientMesh({ page }: { page: string }): JSX.Element;
```

From frontend/src/styles/tailwind.css:
- glass-bold: frosted glass utility
- glass-mobile: reduced blur glass for mobile
- clay-interactive: raised interactive element utility
- clay-pressed: pressed state utility
- glass-refraction-hover: hover refraction (desktop only)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glass treatment for Header and Footer</name>
  <files>frontend/src/components/common/Header.tsx, frontend/src/components/common/Footer.tsx</files>
  <action>
Read both files fully before modifying.

**Header.tsx:**
The header already has some glass-like styling. Upgrade to full glass treatment:

1. Replace existing background classes on the header bar element with `glass-bold`:
   - The header is a fixed/sticky element at the top. It should use `glass-bold` for the frosted glass effect.
   - Per user decision: "Glass header: frosted glass bar, content scrolls behind it, backdrop-blur"
   - The existing header likely has backdrop-blur and bg-opacity classes -- replace with `glass-bold` utility.
   - Keep the existing fade-to-transparent-on-scroll behavior if it exists (see commit history: "header fades to transparent on scroll").
   - If the header has scroll-based opacity, the glass effect should be full when at top and fade when scrolled. This may require conditional class application: `glass-bold` when not scrolled, reduced opacity when scrolled.

2. Apply `clay-interactive` to the theme toggle button and mobile menu button for tactile feel.

3. Add `glass-refraction-hover` class to the main header bar (desktop hover effect -- subtle refraction on the glass surface when hovered).

4. Ensure the header looks correct in both light and dark modes.

**Footer.tsx:**
1. Replace existing background/border classes on the footer container with `glass-bold`.
   - Per user decision: "Glass footer: matches header aesthetic, frosted glass bar"
2. Apply `clay-interactive` to footer link buttons/interactive elements.
3. Ensure footer funder logo cards use `glass-bold` styling.
4. Both light and dark modes must look correct.

Do NOT nest glass layers -- if the footer contains Card components from shadcn, remove the card's default background and let the footer's glass-bold be the single glass surface, or use transparent backgrounds on inner cards.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npm run build 2>&1 | tail -10 && npx vitest run src/__tests__/styles/ --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Header uses glass-bold with frosted glass bar effect, clay-interactive on toggle/menu buttons. Footer uses glass-bold matching header aesthetic. Both work in light and dark modes. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add gradient mesh and glass containers to OCSR, About, Privacy, Terms pages</name>
  <files>frontend/src/pages/OCSRPage.tsx, frontend/src/pages/AboutPage.tsx, frontend/src/pages/PrivacyPolicy.tsx, frontend/src/pages/TermsOfService.tsx</files>
  <action>
Read all 4 files fully before modifying.

**For each page, apply this pattern:**

1. Import `GradientMesh` from `@/components/common/GradientMesh`
2. Add `<GradientMesh page="{pageName}" />` as first child of the page wrapper
3. Wrap major content sections in `glass-bold rounded-2xl` containers
4. Apply `clay-interactive` to buttons and interactive elements
5. Keep all existing animations and motion variants

**OCSRPage specifics:**
- `<GradientMesh page="ocsr" />` (emerald->cyan palette)
- Wrap the OCRView component area in glass-bold container
- The info panel Cards already exist from Phase 4 -- wrap them in glass-bold or ensure they sit on the glass surface correctly
- The external links (DECIMER buttons) should get `clay-interactive` styling

**AboutPage specifics:**
- `<GradientMesh page="about" />` (slate->blue palette)
- Wrap each section (About, Team, Technologies, Funders) in glass-bold containers
- Funder logo Cards should sit cleanly within or on glass surfaces (no nested glass layers)
- Section headings and content should remain readable against glass background

**PrivacyPolicy specifics:**
- `<GradientMesh page="privacy" />` (neutral gray palette)
- Wrap the main content area in a single glass-bold container
- The document text should be readable -- ensure sufficient contrast with glass background
- Links within the policy should get subtle clay-interactive or hover effects

**TermsOfService specifics:**
- `<GradientMesh page="terms" />` (neutral gray palette)
- Same treatment as PrivacyPolicy -- single glass-bold container around content
- Readable text, proper contrast

For text-heavy pages (Privacy, Terms), ensure the glass-bold background opacity doesn't reduce readability. These pages use white/10 background which should be transparent enough for the gradient mesh to show through while keeping text readable.
  </action>
  <verify>
    <automated>cd /Volumes/Data_Drive/Project/2025/cheminformatics-microservice/frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>All 4 pages have gradient mesh backgrounds and glass containers. OCSRPage has emerald-cyan gradient. AboutPage has slate-blue gradient. PrivacyPolicy and TermsOfService have neutral gray gradients. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete design system across all 9 pages</name>
  <files>n/a</files>
  <action>
Present the full visual design system for human verification. All automated implementation is complete at this point. This checkpoint verifies the aesthetic quality and functional correctness that cannot be tested automatically.
  </action>
  <verify>Human visual inspection</verify>
  <done>Human confirms all 9 pages look correct with glassmorphism, bento grids, gradient meshes, and claymorphism in both light and dark modes.</done>
  <what-built>Complete glassmorphism + liquid glass + bento grid visual design system across all 9 pages. Glass header/footer, gradient mesh backgrounds per page, bento grid tool navigation, molecule card enhancements, resizable panels, comparison mode with "Add to Compare" button.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm run dev`
    2. Open http://localhost:3000 in Chrome

    **Glass + Gradients (all pages):**
    3. Visit each page and verify gradient mesh background visible through glass panels:
       - Home: blue-purple gradient
       - Chem (/chem): sky-teal gradient
       - Convert (/convert): indigo-blue gradient
       - Depict (/depict): violet-pink gradient
       - OCSR (/ocsr): emerald-cyan gradient
       - Tools (/tools): amber-orange gradient
       - About (/about): slate-blue gradient
       - Privacy (/privacy): neutral gray gradient
       - Terms (/terms): neutral gray gradient
    4. Toggle dark mode -- verify same glass intensity but shifted color palette (jewel tones)
    5. Verify header is frosted glass with content scrolling behind it
    6. Verify footer matches header glass aesthetic

    **Bento Grid (tool pages):**
    7. On ChemPage: verify 14 tools in editorial bento grid (2 hero-sized, 4 medium, 8 compact)
    8. Click a tool card -- verify it expands in-place with smooth spring animation
    9. Verify other cards fade to 30% opacity during expansion
    10. Click close button -- verify card collapses back
    11. Test same on ConvertPage (3 tools), DepictPage (4 tools), ToolsPage (4 tools)

    **Molecule Cards:**
    12. On HomePage, hover over molecule cards -- verify glow + lift effect
    13. On HomePage, verify feature cards have glass-bold styling and clay-interactive raised feel

    **Resizable Panels:**
    14. Expand a tool on ChemPage on desktop -- verify input/output panel divider appears (if tool uses inputContent/outputContent)
    15. Drag the divider -- verify panels resize
    16. Refresh page -- verify panel sizes persist

    **Comparison Mode:**
    17. Expand a tool on ChemPage -- verify "Add to Compare" button visible in card header
    18. Click "Add to Compare" -- verify comparison tray slides up from bottom
    19. Add a second molecule -- verify "Compare Side-by-Side" button becomes enabled
    20. Click compare -- verify side-by-side view shows both molecules

    **Mobile:**
    21. Open DevTools, toggle device toolbar to mobile viewport (375px)
    22. Verify reduced blur, no SVG refraction, single-column bento, stacked panels

    **Liquid Glass Refraction (desktop only):**
    23. On desktop, hover over a glass panel -- verify subtle refraction distortion appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` -- production build succeeds
- `cd frontend && npx vitest run --reporter=verbose` -- all tests pass
- All 9 pages have gradient mesh backgrounds
- Header and footer use glass-bold
- OCSR, About, Privacy, Terms pages have glass containers
- Human visual verification confirms design quality
</verification>

<success_criteria>
- All 9 pages display cohesive glassmorphism aesthetic in both light and dark modes
- Header and footer are frosted glass bars
- Bento grids work on all 4 tool pages and HomePage
- Gradient mesh colors match per-page specifications
- Interactive elements have claymorphism accents
- Mobile displays optimized glass effects (reduced blur, no refraction)
- Comparison mode is functional with "Add to Compare" button on ChemPage
- Human approves visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-visual-design-system-glassmorphism-liquid-glass-bento-grids/04.1-06-SUMMARY.md`
</output>
